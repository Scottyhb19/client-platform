# Go-Live Checklist — project-wide pre-launch gate (Gate 3 deliverable)

**Status (2026-07-21):** **every beta-gating item is CLOSED.** The §1 Supabase Pro cutover sitting ran 2026-07-21: DR drill passed, HIBP on (G-3 GREEN behaviourally, G-3u GREEN — C-7 closed), session time-box set to 30 days (G-4), §7 auth-config re-run done at the same sitting. Also closed: §2 (secrets), §4 (SECURITY DEFINER sweep), §4b (anon table grants), §5 (non-prod target), and the §8 Vercel-Pro perf residual (upgrade landed, `syd1::syd1` verified). §3 closed the same day (post-reset sibling-session revocation shipped + two-browser verified), so **no beta-gating or beta-scoped item remains open — the checklist is fully walked at f&f scope.** Standing: §5b (render-tier decision), §6 (re-run rule). §8 items are paying-client-gated or feature-triggered pointers, not beta gates. Note: real f&f data preceded the DR drill by ~41 days — recorded as an exposure window in `incident-response.md` §10, per the CLAUDE.md rule.

**What this is.** The named dev-to-production transition gate referenced by `schema.md` and `slos.md` as the Gate 3 deliverable. It is the project-wide gate that must be walked before the first real user — including any friends-and-family beta tester — creates data.

**What this is NOT.** Not a restatement of the documents it points at. Every item below is either a verification gate (confirm a property holds, with a pointer to the authoritative document for detail) or a trigger surfaced at the point it fires. Where a liability is owned by another document, this checklist points at it and does not duplicate its detail — a duplicated gate goes stale the day its source changes. This file deals in gates and document pointers only; it carries no file:line citations, by design, because every line-cited document in this repo has drifted at least once.

**How to read it.** The hard rule is absolute and gates everything else. The remaining sections are the verification and trigger gates, in no priority order — all must hold before first real data.

**The single-ledger rule (2026-07-22).** This checklist is the **only current-state ledger** for deferred, re-triggered, and tracked-separately **hardening, security, and verification** items out of closed sections; **feature and UX deferrals** index in `docs/deferred-prompts.md` instead. Two halves, both binding: (1) *inbound* — an item deferred at any section sign-off lands in one of those two indexes in the same commit, or it does not exist (restates CLAUDE.md's Technical-gate-index rule: a re-trigger recorded only in a closed polish doc does not fire); (2) *outbound* — closing, materially advancing, or re-triggering any indexed item updates its index entry **in the same commit** as the change. A closed section's polish doc becomes a historical record at sign-off — **with one named exception (2026-07-22): sign-off freezes the narrative, not reopened gaps.** When a signed-off section's gap is formally reopened (a reviewer return, a new gap number) or a deferred item advances to a pending state (built-on-staging awaiting prod apply), the closure record for that item still lands in the owning polish doc as an appended, dated section — because that is where its evidence chain lives — with this checklist's entry updated in the same commit. What the rule forbids is re-editing the frozen narrative or treating a polish doc's sign-off-era deferred list as current state; it does not forbid appending. (Without this exception the rule would have broken on its first real use: G-15 and the G-6 prod apply, both live in `polish/auth-onboarding-staff.md`, both of which must record their closures somewhere.) Provenance: the 2026-07-22 staff-auth reviewer pass, where a six-week-stale section ledger (R-4 "never run" — it had run twice; G-3/G-4 "deferred" — both closed at the Pro cutover) cost a full review cycle in false blockers; the same-day ten-doc drift audit found the same class of staleness in two more docs and four unindexed deferrals, all reconciled that day.

## 0. The hard rule (source of truth: `CLAUDE.md` "Open gates")

No paying clinical client may be onboarded to OdysseyHQ as their primary clinical record system until all three of the following are true:

- **(a)** An external IT security review is completed and documented in `docs/external-reviews.md`.
- **(b)** Anthropic has established a BAA meeting Australian health-privacy standards (assessed 12–24 month horizon; treat as not-yet-met until documented).
- **(c)** The entity structure has been reviewed against the increased liability surface (sole trader to likely Pty Ltd).

`CLAUDE.md` is the source of truth for this rule; the text above is reproduced for legibility at the gate. If the two ever diverge, `CLAUDE.md` wins. Until all three hold, the existing clinical clients stay on Cliniko and OdysseyHQ runs as a friends-and-family beta only. This rule is not waivable by convenience, deadline, or "just one client".

Note the scope distinction this checklist runs on: the friends-and-family beta itself begins the day the first beta tester logs in. Items below marked **before first real data** gate that day. Items marked **before paying client** gate the hard-rule threshold above, which is later. The two are not the same day.

## 1. Supabase Pro-tier transition (owned by: `auth-onboarding-staff.md` Track A)

The free tier is acceptable only while no real data exists. The following are Pro-gated and are reproduced here only as the go-live trigger; Track A owns their detail and current state:

- Point-in-Time Recovery and daily backups become mandatory the moment a real client is onboarded (see `schema.md`, `slos.md`).
- **G-3** — HIBP leaked-password protection. Pro-gated; confirmed locked on the free tier (verified 2026-06-10, `auth-onboarding-client.md` C-7 — the toggle cannot be enabled at all below Pro). Enable at Pro cutover.
- **G-4** — 30-day refresh-token lifetime. Pro-gated. Set and record at Pro cutover.
- **Client session duration (open question 1, `auth-onboarding-client.md`)** — never formally resolved; current state is 30-day uniform across roles. Decide deliberately at Pro cutover, in the same dashboard visit as G-4 (master brief §4.2 contemplated shorter client sessions for shared devices).
- First DR drill is run on the newly-upgraded Pro project as the final pre-launch step (named in `schema.md` and `slos.md`). **Drill procedure now written — `docs/disaster-recovery.md` (the "Restore to a New Project" recipe + a verification census + teardown).** Confirmed 2026-07-02 the project is still on **Free**, so the drill is **blocked until this Pro upgrade** — a documented dependency, not a skip. Do not mark the DR gate met until the drill actually runs post-upgrade and its result is logged in `disaster-recovery.md`.

**Gate: CLOSED 2026-07-21 — the Pro cutover sitting.** All five parts ran in one sitting on the upgraded Pro project:
- **DR drill: PASSED** — restore to a scratch project, census matched prod (RLS policies intact), teardown confirmed. Log: `disaster-recovery.md` run log. Because real f&f logins preceded the drill (first: 2026-06-10), the gap is recorded as an exposure window in `incident-response.md` §10 — the gate closed *after* first real data, honestly recorded, not waved.
- **G-3 HIBP: ON and GREEN behaviourally** (front-door signUp rejected a breached password), and **G-3u GREEN — the definitive C-7 answer: HIBP fires on `updateUser`**, the path every client welcome/reset uses. Recorded in `runbooks/verify-auth-config.md` (2026-07-21 run-log row).
- **G-4: SET** — the control is now the dashboard's "Time-box user sessions"; set to 30 days (720 h), verified by Management API re-read (`sessions_timebox: 720`). Recorded in the runbook.
- **Client session duration (open question 1): DECIDED** — 30-day uniform across roles at f&f scope (f&f clients are on personal devices, not the shared-device case the brief worried about). Re-trigger: revisit at the paying-client gate.
- **Backups posture: Pro daily backups (7-day retention); PITR deliberately deferred to the paying-client gate** (paid add-on; 24 h RPO accepted at f&f scale). Deviation recorded in `slos.md` §2.2.

## 2. Secrets (source of truth: `secrets-inventory.md`, `secrets-rotation-log.md`)

Standing policy is rotate-on-suspicion-of-exposure; there is no scheduled cadence. State at this writing:

- `RESEND_API_KEY` and `CRON_SHARED_SECRET` were rotated 2026-05-17 after transcript exposure — already discharged.
- `SUPABASE_SERVICE_ROLE_KEY` — **DONE 2026-07-02.** Migrated to a new `sb_secret` API key and the legacy keys were disabled, so the leaked legacy `service_role` JWT is now rejected by the gateway. This was the one open secrets item; it is now **closed**. Detail: `secrets-rotation-log.md` (2026-07-02 entry).
- **Edge Function reminder secrets — gap found 2026-06-16 (§9 Edge Function verification).** The `send-appointment-reminders` Edge Function runs on Supabase with its OWN secret set, which the 2026-05-17 `RESEND_API_KEY` rotation never reached — the deployed function held the stale (now-invalid) key → `resend 401 validation_error` → **reminders could not send** (FM-3 unmitigated in production, silent). Also absent from the EF's Supabase env: `EMAIL_FROM` (→ EF threw → HTTP 500 on every invoke) and `NEXT_PUBLIC_APP_URL` (→ reminder-email CTA fell back to `#`). `EMAIL_FROM`, `NEXT_PUBLIC_APP_URL`, and `RESEND_API_KEY` (the current, verified key) were all set on the EF's Supabase secret set 2026-06-16, and a reminder send was **verified end-to-end** (a due reminder to `delivered@resend.dev` reached `status='sent'` with a Resend `provider_message_id`; detail in `docs/polish/scheduling.md` §8c). The before-relying-on-reminders send gate is **MET**. **Consumer sweep — DONE (2026-06-17, `secrets-rotation-log.md`):** enumerated every runtime reader of `RESEND_API_KEY`; exactly two stores exist (Vercel/`.env.local` → `src/lib/email/client.ts`; Supabase EF secrets → the reminder function), both now hold the current key, no third consumer. A standing post-deploy **synthetic send check** (`runbooks/deploy-an-edge-function.md`) was added so the enqueue-vs-send blind spot cannot recur. **DONE 2026-07-03 (operator dashboard check):** Resend → API Keys shows exactly one active key, created 2026-05-17 — the current rotated key; no stale pre-rotation key remains (`secrets-rotation-log.md`, closure note under the 2026-06-16 entry). **Lesson, now folded into `runbooks/rotate-a-secret.md`:** a rotation must enumerate the Edge Function's Supabase secret set, not just `.env.local`/Vercel — they are separate stores, and the EF reads `EMAIL_FROM`/`NEXT_PUBLIC_APP_URL`/`CRON_SHARED_SECRET` there too.
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` is not a secret (RLS-bounded, shipped to the browser by design) — it is not a rotation item. It was nonetheless migrated to the new `sb_publishable` key on 2026-07-02 (the legacy-disable toggle covers both keys, so anon had to move too).

**Gate:** ~~before first real data, confirm or rotate `SUPABASE_SERVICE_ROLE_KEY`~~ — **MET 2026-07-02** (migrated to `sb_secret` + legacy keys disabled; outcome recorded in `secrets-rotation-log.md`). Catalogue in `secrets-inventory.md`; the procedure actually run is in `runbooks/rotate-a-secret.md`.

Tracked secrets tech-debt (pointers, not gates): ~~the `CRON_SHARED_SECRET` pg_cron value is an inline literal, not Vault~~ — **DONE 2026-07-01** (migration `20260701120000` moved the token to Supabase Vault, read per-tick via `vault.decrypted_secrets`; `cron.job.command` no longer contains it, and the 2026-07-02 `message-notifications-5min` job reuses the same Vault entry — detail in `runbooks/deploy-an-edge-function.md`). `NEXT_PUBLIC_SITE_URL` and `NEXT_PUBLIC_APP_URL` are the same logical value under two keys and must both be set and kept in sync (Flag E, runbook README backlog) — **verified in sync 2026-07-03**: Vercel has both set across all environments (operator dashboard check), and a shadowing duplicate in `.env.local` (the prod pair re-declared after the localhost pair; dotenv last-key-wins pointed local dev auth callbacks and booking links at prod) was removed the same day. The keep-in-sync invariant remains standing until the two keys are consolidated.

## 3. Auth security forward-dependencies (source of truth: `auth-onboarding-staff.md` Track C)

The Track C security pass created or assumes these production-config dependencies:

- Production-origin env var must be set. The open-redirect fix made auth depend on `getPublicOrigin()`, which throws if `NEXT_PUBLIC_SITE_URL` is unset. Auth now fails closed if it is missing — login, confirmation, and recovery all break entirely. Confirm it is set in production before launch. **Confirmed in production 2026-06-10** after this exact failure fired live (second incident record, `auth-onboarding-client.md`): both vars set in Vercel, `/api/health` now reports this class continuously, and `runbooks/deploy-the-app.md` makes the health check a standing post-deploy step.
- HttpOnly cookies and refresh-token rotation must be active. **Verified 2026-07-02 — one half confirmed, one half re-based on ground truth.** (a) **Refresh-token rotation: ACTIVE**, verified behaviourally on live (mail-free admin-created probe user: sign-in → refresh issued a *distinct* single-use refresh token; probe torn down). (b) **HttpOnly: NOT the posture, and architecturally cannot be** — `@supabase/ssr`'s cookie default is `httpOnly: false` (confirmed in the installed package; the app sets no override) because the browser client must read the session cookie (the C-1 FinishSetup recovery path depends on it; flipping it would break that flow). The original line's assumption was doc drift — `auth.md` (forbidden-practices table) claimed "HTTP-only cookies (Supabase default)" and was corrected 2026-07-02. The CSRF/XSS posture actually in force: SameSite=Lax cookies, Next server-action origin checking, `getUser()` server-side re-verification, no service key client-side, and single-use rotating refresh tokens. **Flagged for the external auth review (hard rule (a)) — reviewer should assess the non-HttpOnly cookie posture explicitly.**
- Post-reset session behaviour — **CLOSED 2026-07-21.** Decision: revoke-siblings-keep-current (`auth.signOut({ scope: 'others' })` after the successful password write), applied to both the reset action and the welcome set-password action for parity; bounce-to-login (global revoke) was considered and rejected as friction without attacker-facing gain. Verified live by a two-browser operator pass (`RESET-REVOKE-1/2`, `test_scenarios_template.md`); the underlying doc-drift (auth.md §5.5 wrongly claimed GoTrue revokes by default) corrected the same day. Best-effort semantics: a revoke failure logs and never blocks the reset.

**Gate: all three CLOSED** (production-origin env confirmed 2026-06-10; rotation verified / HttpOnly re-based 2026-07-02; post-reset behaviour shipped and verified 2026-07-21).

## 4. SECURITY DEFINER function grants (verify-gate)

The SECURITY DEFINER signup/invite functions (`create_organization_with_owner`, `staff_create_client_invite`, siblings, and the Track C `consume_recovery_ticket`) are hardened in migration source: each does `REVOKE EXECUTE FROM PUBLIC` then `GRANT EXECUTE TO authenticated`, and `seed_organization_defaults` revokes from `PUBLIC, authenticated, anon` explicitly. Anon is not granted execute on any of them in source.

This was confirmed against migration source, not against the live database — no live-query path existed at the time of writing.

**Gate (verify, not remediate):** once a live query path exists (a wired SQL connector, or `psql`), confirm the runtime grants match source — anon absent — by reading `information_schema.role_routine_grants` for these functions. If runtime matches source, this is closed by confirmation. If runtime diverges (anon present), treat as remediation at that point. Do this before client health data enters the project.

**Section-3 RPC family added to this sweep (2026-06-11).** The clinical-records polish pass added three more SECURITY DEFINER functions on the same pattern — `sync_client_profile_name` (CN-5, `20260611130000`), `soft_delete_client_medical_history` and `restore_client_medical_history` (CN-6, `20260611130100`) — each `REVOKE EXECUTE … FROM PUBLIC` then `GRANT EXECUTE … TO authenticated` in source, anon never granted. These join the soft-delete RPC family (`20260429120000` / `20260429130000`). The `role_routine_grants` sweep above must enumerate the **whole** SECURITY DEFINER family, not just the signup/invite functions, and confirm anon is absent for every one. Per the section-3 security recon (2026-06-11): runtime anon-EXECUTE is **unverified across the entire family** — the REVOKE-FROM-PUBLIC idiom does not strip a role-specific anon grant that Supabase default privileges may have added, so source-absence is necessary but not sufficient. Each function's in-body auth guard (`organization_id`/role check, or self-only re-read) is the load-bearing protection until the sweep runs.

**Section-5 confirmation + guardless-helper priority (2026-06-12).** The program-engine/session-builder pass *confirmed the section-3 hypothesis is real at runtime*: a read-only `has_function_privilege('anon', …, 'EXECUTE')` probe (committed at `docs/Prompts/section5-verification.sql`, check 6) showed `anon` **does** hold direct EXECUTE on program RPCs despite `REVOKE … FROM PUBLIC` in source. The six functions that pass created/replaced were tightened immediately (migration `20260612130000`: `REVOKE … FROM anon` on all six, plus `FROM authenticated` on the guardless internal `_clone_program`). **New, sharper finding for the sweep:** the in-body guard that makes the lingering anon grant harmless is *absent on internal `_` helpers*. `_clone_program` (write; now fixed) and **`_program_for_date`** (`20260503120000`; read-only — returns a program id; still anon-reachable, NOT yet fixed) are guardless. **The sweep must treat guardless internal SECURITY DEFINER helpers as the highest-priority bucket** — for them, anon-grant presence is not defence-in-depth, it is the whole exposure. Use the `has_function_privilege`/`pg_get_functiondef` probe shape from `section5-verification.sql` to enumerate every SECURITY DEFINER function, flag those whose body lacks a `user_organization_id()`/`user_role()` guard, and revoke anon from those first.

**Section-6 partial discharge + new finding (2026-06-12, program-calendar P0-1).** Migration `20260612150000` revoked anon EXECUTE on the **entire remaining program-engine/calendar family** predating `20260612130000` (ten guarded caller-facing functions: `copy_program`, `repeat_program`, `create_program_day`, `duplicate_program_day`, `soft_delete_program_day`, `soft_delete_program_exercise`, `restore_program_exercise`, `soft_delete_program_exercise_set`, `reorder_program_exercises`, `swap_program_exercise`) and made the guardless **`_program_for_date` definer-only** (anon AND authenticated revoked — closes the priority item above). pgTAP `23_program_rpc_grants.sql` (21/21 green on live, 2026-06-12) now locks the whole family's posture in as a regression tripwire — any future `CREATE OR REPLACE` that re-trips the auto-grant fails the suite. **The program family is done; the platform-wide sweep remains open**, and the same live probe surfaced its most urgent remaining bucket: **the `_test_*` pgTAP fixture helpers are anon-executable on the live project** — `_test_make_user(text)`, `_test_grant_membership(uuid, uuid, user_role)`, `_test_set_jwt(uuid, uuid, text)`, `_test_clear_jwt()`, `_test_insert_test_session(...)`, `_test_insert_test_result(...)`, `_test_insert_client_publication(...)`. Several are SECURITY DEFINER **write** helpers built to bypass normal flow for fixtures (user creation, membership grants, data inserts); anon-reachable, they are a tenant-boundary bypass, not defence-in-depth. They predate this section and are out of its contract — flagged to the operator 2026-06-12; remediation shape: revoke anon + authenticated (the pgTAP runner connects as the database owner, so test runs don't need API-role grants — verify the suite still passes after the revoke), or drop them from prod entirely once the §5 non-prod target exists. **RESOLVED 2026-06-12 (operator-approved, folded into program-calendar P1-1):** migrations `20260612160000` §3 + `20260612160100` (the helpers had no PUBLIC revoke — API roles resolved EXECUTE through the PUBLIC grant) plus a self-securing rewrite of `supabase/tests/database/00_test_helpers.sql`: every helper now carries an in-body `session_user = 'postgres'` guard that blocks PostgREST sessions regardless of grants and survives future auto-grant re-trips; the five fixture writers are owner-only at the grant level, the two JWT spoofers keep an authenticated grant solely for the suite's `SET LOCAL ROLE` pattern. pgTAP 23 §D locks the posture in; full scriptable suite re-ran 176/176 green. Full detail in `docs/polish/program-calendar.md` progress log.

**Section-7 partial discharge + new findings (2026-06-14, client-portal-pwa P0-2).** A live `has_function_privilege('anon', …, 'EXECUTE')` probe of the whole `client_*` family confirmed all of them were anon-executable. The **section-7 client-portal family** was revoked: migration `20260614120000` (reschedule v3) + `20260614130000` cover `client_start_session`, `client_log_set`, `client_complete_session`, `client_get_week_overview`, `client_get_program_day_exercises`, `client_get_published_reports`, `client_owns_test_session`, `client_list_program_days`, and `client_reschedule_program_day_to_today` — `REVOKE … FROM anon`, authenticated retained. pgTAP `25_portal_rpc_grants.sql` (18 assertions) is the regression tripwire. **The probe surfaced four more anon-reachable functions owned by other, separately-gated sections — still open in the platform-wide sweep, deliberately NOT revoked in section 7:** `client_accept_invite(uuid)` (**§2 onboarding — VERIFY whether it is ever called pre-authentication before revoking anon; a blind revoke could break the invite-accept flow**); `client_available_slots(timestamptz, timestamptz)`, `client_book_appointment(uuid, uuid, timestamptz, timestamptz)`, `client_cancel_appointment(uuid)` (**§9 scheduling**); and `client_cascade_thread_archive()` (**§10 messaging — no-arg; confirm it is a trigger/internal helper, not caller-facing, then revoke anon + authenticated**). These join the still-open platform-wide sweep; each is protected in the meantime by its in-body auth guard (the load-bearing protection until revoked).

**Section-9 partial discharge (2026-06-15, scheduling P0-1).** The scheduling polish pass re-probed live (`has_function_privilege`) and confirmed the three §9 booking RPCs flagged above were anon-executable, plus a fourth scheduling-family SECURITY DEFINER write the same probe surfaced — `soft_delete_availability_rule(uuid)`. Migration `20260615120000_revoke_anon_execute_scheduling_rpcs.sql` revoked anon EXECUTE on all four (`client_available_slots`, `client_book_appointment`, `client_cancel_appointment`, `soft_delete_availability_rule`); authenticated retained; backward-compatible (grants only, applied to live 2026-06-15). pgTAP `26_scheduling_rpc_grants.sql` (8/8 green on live, 2026-06-15) is the regression tripwire. No guardless internal `_`-helper exists in the scheduling family (all four are guarded SECURITY DEFINER, fail closed for anon); `appointments_client_field_lockdown` is a non-invocable trigger function, left untouched. **The §9 scheduling-RPC slice is discharged.** The platform-wide sweep remains open: `client_accept_invite(uuid)` (§2 — still needs the pre-auth-use verification before any revoke) and `client_cascade_thread_archive()` (§10) stay owned by their sections. Detail in `docs/polish/scheduling.md` §7 (P0-1).

**Soft-delete / restore family discharge + first full runtime enumeration (2026-06-23, post-polish dogfooding sweep).** The first live `has_function_privilege('anon', …, 'EXECUTE')` enumeration of **every** SECURITY DEFINER function in `public` (not one family at a time) was run — the runtime-verify step this gate has called for since 2026-06-11. It confirmed the section-3 hypothesis across the board: **36 SECURITY DEFINER functions held a direct anon grant** from the default-privilege trap despite `REVOKE … FROM PUBLIC` in source.

*Discharged this pass — the staff-only soft-delete / restore family for the test / client / clinical / library tables*, the members never caught by a prior section sweep. Migration `20260623180000_revoke_anon_soft_delete_restore_family.sql` revoked anon EXECUTE on **18** functions — 11 `soft_delete_*` (`client`, `client_medical_history`, `client_publication`, `clinical_note`, `exercise`, `exercise_tag`, `movement_pattern`, `practice_custom_test`, `test_battery`, `test_result`, `test_session`) and 7 `restore_*` (`client`, `client_medical_history`, `clinical_note`, `practice_custom_test`, `test_battery`, `test_result`, `test_session`); authenticated retained; applied to live 2026-06-23. pgTAP `38_soft_delete_restore_grants.sql` (**38/38 green on live**) locks the family's posture (19 functions incl. the orphan `restore_client_publication`, which had no prior tripwire). The two earlier same-day fixes — `client_log_set` (`20260623160000`) and `soft_delete_program_template` (`20260623170000`, pgTAP 36 §A7) — are part of this sweep. **This closes the "Section-3 RPC family" item (2026-06-11).** The full delete/undelete family (26 members across this pass + tests 23 / 26 / 36) now reads anon=false / authenticated=true at runtime.

*Enumerated + classified, NOT remediated this pass — each legitimately anon or owned elsewhere:*
- **anon REQUIRED — leave granted:** `consume_recovery_ticket` (pre-auth reset-password path), `rate_limit_check_and_record` / `rate_limit_check_failures` / `rate_limit_record_failure` (pre-auth throttles), `calendar_feed_events` (the unauthenticated `.ics` feed — locked anon=**true** by pgTAP 32). This is the runtime "divergence" the gate's opening paragraph anticipated for the signup/invite/recovery set — but for these pre-auth functions the anon grant is *correct*, not a hole; source-absence was the wrong expectation for them.
- **authenticated REQUIRED (RLS-policy helpers) — anon revoke is a §8 call:** `battery_in_clients_published_session`, `test_metric_visibility`, `test_session_in_org`, `test_session_has_active_publication`, `test_session_has_auto_visible_metric` — all referenced in `test_*` RLS policies (authenticated-only roles), so anon never reaches them; revoking anon is marginal-benefit / real-regression-risk → testing-module owner, with its own tripwire.
- **inert — trigger return type, not RPC-callable:** `handle_new_auth_user`, `log_audit_event`. No action.
- **candidate, owner-gated:** ~~`audit_resolve_org_id` + `assert_audit_resolver_coverage` … `create_organization_with_owner` + `staff_create_client_invite` … `client_accept_invite`~~ — **DISCHARGED 2026-07-02** (migration `20260702130000`). The pre-auth-use verifications ran first: `client_accept_invite`'s sole runtime caller is the post-session `/welcome` action (full verification record: `polish/auth-onboarding-client.md`, Deferred-item closures); `create_organization_with_owner`'s sole caller sits behind the auth middleware + its own `getUser()`; `staff_create_client_invite` has no runtime caller left (invite flow moved to admin `generateLink`). All three: anon revoked, authenticated retained. The audit internals went **definer-only** (anon AND authenticated revoked) — safe because every audit function is postgres-owned (the hosted ownership-transfer block, `20260420102300`), so `log_audit_event`'s inner calls execute as postgres; `audit_trim_row` + `audit_diff_fields` were folded in as same-family internals (absent from the 2026-06-23 enumeration only because they are not SECURITY DEFINER; live-probed holding the same anon grant). `log_audit_event` itself untouched (inert, per above). pgTAP `52_onboarding_audit_rpc_grants.sql` (14/14 on live) is the tripwire; pgTAP `51` re-ran green post-revoke proving the audited-write chain intact. Grants-only migration — no RLS touched, so the §6 cross-tenant re-run rule was not tripped (test 17 last ran green 2026-07-02 after the API-key migration).

**Gate status: CLOSED 2026-07-02.** The runtime-verify step is DONE (platform-wide enumeration, 2026-06-23), every family is discharged — soft-delete/restore + program (23) + portal (25) + scheduling (26) + messaging + onboarding/audit (52, `20260702130000`) — and the candidate bucket above is discharged. What remains is **not remediation but standing classification**: the anon-REQUIRED pre-auth functions (correct by design, locked by their own tests) and the §8-owned RLS-policy helpers (testing-module owner's call, marginal benefit). Any future SECURITY DEFINER function joins the sweep via its section's pgTAP grants tripwire — the pattern is established (tests 23/25/26/38/52).

## 4b. Anon table-grant posture on tenant tables (defense-in-depth, parked)

Tenant tables (e.g. `clients`) keep the Supabase **default table-level grant** for
`anon`; anon is denied not at the grant layer but by RLS (there is no `TO anon`
policy). pgTAP `46_clients_update_role_anon_denial` proves this holds today for the
`clients` UPDATE path (anon UPDATE affects 0 rows, not a grant error). The posture
is **acceptable** (RLS is the security boundary by design) but not the intended end
state: anon retaining table grants means a single RLS regression is all that stands
between anon and the row. Tightening means `REVOKE`-ing the unused anon table
privileges so anon is denied at both the grant and RLS layers. This is distinct from
the §4 *function*-EXECUTE sweep (which covers SECURITY DEFINER functions, not table
DML).

**Gate: CLOSED 2026-07-02 (tightened).** Migration `20260702170000` revoked anon's
table grants across public — the live probe found 473 grants over 68 tables/views
with **zero** RLS policies targeting anon — plus anon's sequence privileges, and
(deliberate, flagged extension) the **postgres-role default privileges** for anon on
future tables, sequences, and functions: the function-defaults entry was the root
cause of the §4 anon-EXECUTE trap five sections fought family-by-family, so objects
are now born without anon grants and a future pre-auth surface must `GRANT … TO anon`
explicitly. Pre-auth surfaces verified unaffected before the revoke (invite gate +
`/api/health` use the service-role client; `consume_recovery_ticket` / `rate_limit_*`
/ `calendar_feed_events` are SECURITY DEFINER). anon DML now raises 42501 at the
grant layer instead of matching zero rows through RLS — pgTAP `46` updated to the
contingency its own header pre-wrote, and pgTAP `54_anon_table_grants.sql` (8/8 on
live, **dynamic** catalog scans, no hard-coded table list) is the standing tripwire.
Re-runs green post-revoke: 46 (3/3), 17 cross-tenant (8/8), 32 calendar feed (8/8 —
the one deliberate anon function grant survives), prod `/api/health` 200/db-ok.
Residual: `supabase_admin`'s default ACL cannot be altered from the postgres role;
any table it might create in public is caught by test 54's dynamic scan.

## 5. Non-prod test target — CLOSED 2026-07-03

~~All pgTAP runs only against the live production Supabase project via `BEGIN ... ROLLBACK`. There is no non-prod test target.~~ **CLOSED 2026-07-03.** The staging project `odyssey-staging` (`fbtfzlgvnivgwydlijka`, ap-southeast-2, Free tier) is stood up and proven: all 176 migrations applied clean; the full 56-file pgTAP suite runs green against it (`scripts/run-pgtap-staging.sh`); and a genuinely destructive operation (`db reset --db-url`, replay + documented recovery) was rehearsed with **zero prod impact** (prod verified untouched after the drill: migration count, cron jobs, data). Procedure, the two order-of-operations discoveries (pg_cron/pg_net apply-time dependency; `00_test_helpers.sql` before-AND-after-push), and the free-tier wake-from-pause caveat: `runbooks/use-the-staging-project.md`.

Day-one value proof: the first full-suite staging run caught pgTAP `49` asserting the pre-CN-7 archived-client posture (stale on prod too — the CN-7 pass re-ran 17/38/46/54/56 but missed 49); fixed same day, green on both targets.

**Gate met.** Residual note: Supabase **branching** (Pro-only) is the cleaner successor — reconsidered at the Pro upgrade (§1) as scheduled. **Decision (2026-07-21): keep the free staging project; do not adopt branching.** Rationale: staging is proven (full suite green, destructive drill rehearsed), costs nothing, and branching bills per-branch compute for no gain at solo scale. Re-trigger: revisit if migration cadence grows to where per-PR preview databases would pay for themselves (multi-practitioner development, or the SaaS fork). Free-tier staging auto-pauses after ~1 week idle; wake before use per the runbook.

## 5b. Render-tier verification (standing decision)

There is no authenticated-staff `:3000` smoke harness, so the **render tier** — does the UI actually paint the computed state (an overlap message appears, a 404 page renders, a list row shows) — is browser-unverified for every polish section, and will be for every future one (Sessions, Circuits, …). The tiers *below* render are machine-gated: logic / security / data-integrity by pgTAP on live, types by `tsc`, compile by `next build`. What is not machine-verified is the final pixel paint, because every staff surface sits behind `requireRole(['owner','staff'])` and the preview browser holds no authenticated session.

**Decision (operator, 2026-06-23):** for the friends-and-family beta, render-tier is **formally accepted as verified by `type-check` + `next build` + pgTAP (data layer) + code-read**, not by automated browser. Rationale: solo today → the operator *is* the smoke test (render bugs surface in daily dogfooding); a Playwright authed-staff harness is real upfront + ongoing cost (auth automation, seeded accounts, selector upkeep), over-investment for a pre-beta solo tool. **This is decided once — do not re-litigate per section; cite this entry at each close** ("render-tier accepted at F&F per go-live-checklist §5b").

**Re-trigger — stand up the authed `:3000` smoke harness at the FIRST of:** (a) a second staff account exists (the operator can no longer dogfood every staffer's view), (b) a paying client onboards (Tier 2 — render bugs gain real consequence), or (c) any render-tier bug actually reaches a user. At that point build a Playwright authed-staff harness (seeded staff login, drives the UI, asserts the render-tier claims — overlap message paints, 404 renders, etc.); it then becomes reusable for every section thereafter.

**HARNESS STOOD UP 2026-07-21 (ahead of its re-trigger, as Step 6 of the internal work sequence).** `playwright.config.ts` + `e2e/` (`npm run test:e2e`): logs in through the real `/login` form as the seeded staging staff owner (`STAGING_DEV_LOGIN_*` from `seed-staging.mjs`; refuses to run against anything but staging) and asserts the render tier — dashboard needs-attention triggers, client list, archived profile read-only + Restore (CN-7), the completed-day builder LOCK banner (Step 2's UI half), the Comms tab row + body (§12 Part B), and the G-6 `auth.login.success` row from its own login. **7/7 green on first full run (2026-07-21).** The §5b acceptance decision stands for historical sections; new UI work now has this harness as its render gate, growable per surface. First catch: the seed's days lacked `published_at` — unassigned days can't lock — fixed in the seed + backfilled.

## 6. Cross-tenant isolation regression test (execution gate)

R-4 is closed. The automated pgTAP test `supabase/tests/database/17_cross_tenant_isolation.sql` landed 2026-06-07 and passed 8/8 against the live project — read isolation on `clients`/`clinical_notes`/`programs`, write isolation on `clients` (UPDATE affects 0 rows; foreign-org INSERT raises 42501), plus anti-trivial controls. The manual procedure at `runbooks/verify-cross-tenant-isolation.md` was also run for the first time the same day (all checks pass, recorded in its run log) and is downgraded to a quarterly broader-surface check (it covers all eight core tenant tables; the automated test covers the regression-prone core).

**Gate:** run the automated test (`17_cross_tenant_isolation.sql`) on any migration touching RLS, the JWT hook, or the auth helpers, and before first real data. Since the 2026-07-21 environment-separation flip the **default target is staging** (`supabase db query --linked -f`, or the full-suite runner in `runbooks/use-the-staging-project.md`); a prod re-run (`BEGIN … ROLLBACK`, via the explicit prod workdir channel) belongs to deploy sittings, after the migration lands on prod. Re-run the broader manual procedure quarterly or whenever the automated test's table coverage is in doubt.

## 7. Auth-config verification (cadence gate)

`runbooks/verify-auth-config.md` and `scripts/verify-auth-config.mjs` verify the dashboard-config properties. G-1 (custom-access-token hook) is verified green and automated. G-7 (email confirmations) is verified. G-3 and G-4 remain Pro-gated (section 1).

**Gate:** re-run the auth-config verification on the Pro-upgraded project as part of cutover, and confirm G-1 and G-7 still green after the upgrade. The same run delivers the definitive **G-3u** answer (does HIBP fire on `updateUser`, the path every client password-set and reset uses) — the probe is built and self-arming; with the Pro HIBP toggle on, G-3u GREEN closes the question, RED is the real platform hole and the support-ticket case (`auth-onboarding-client.md` C-7).

## 8. Deferred hardening surfaced at this gate (pointers)

- **G-6** — structured auth-event audit log. ~~Deferred-with-trigger: before any paying clinical client.~~ **BUILT 2026-07-21 (staging), pending prod apply** — pulled forward as Step 3 of the internal work sequence: migration `20260721140000` (`auth_events` append-only table, server-side-only), 8/10 §11 events wired at their app emission points, pgTAP `61` (8/8) tripwire, live-verified end-to-end. The two unwired events (`jwt.hook_failure`, `cross_tenant_access_attempt`) + alert thresholds are honestly recorded in the closure — `auth-onboarding-staff.md` "G-6 closure". **PROD-APPLIED 2026-07-23** (the prod-apply sitting): migration `20260721140000` applied; `auth_events` table + append-only trigger confirmed in the prod catalog with function-body parity vs staging (where pgTAP `61` runs 8/8); API-invisibility probed live on prod (anon SELECT → 42501 at the grant layer). The G-6 record remains **partial by design** (8/10 events, 0/2 alert thresholds — the closure section's own condition). **First-login behavioural check DONE** — the operator's prod login landed `auth.login.success` 2026-07-22 22:45:01 UTC and the same session's `auth.invite.sent` 22:46:31 UTC, read back via the prod workdir. **SIGNED OFF 2026-07-23 — Closed with deferred items (partial)** (reviewer: Claude Opus 4.8; full sign-off stanza + deferred register: `auth-onboarding-staff.md` "Sign-off — G-6"). **Register items indexed here so their re-triggers fire — three CLOSED by the 2026-07-23 parity pass:** (a) **F-4 TRUNCATE blind spot — CLOSED 2026-07-23**: migration `20260723120000` revoked TRUNCATE on `auth_events` from `service_role` (postgres retains it for retention maintenance); pgTAP `61` #9 is the tripwire. (b) **B-4 audit-write failure routing — CLOSED 2026-07-23**: `logAuthEvent` failures now route through the same `captureException` seam `src/lib/comms/log.ts` uses (still best-effort, never rethrown). (c) **F-2b per-IP login-failure threshold — CLOSED 2026-07-23 (computable + alerting; revised same day at the sign-off review)**: `client_ip inet` added (`20260723120000`), captured best-effort in `logAuthEvent` (platform-attested `x-vercel-forwarded-for` first — Vercel-docs-verified unspoofable — with XFF-first-hop/`x-real-ip` as off-platform fallbacks, order vitest-pinned; rows predating the column are honestly outside the window); **and the §11 alerting pipeline itself is BUILT** — `auth_events_threshold_scan()` (`20260723150000`, service_role-only) + the `auth-events-alerts` Edge Function on a 15-minute Vault-token cron (`20260723170000`, cadence 4× the 60-min window per `20260723180000` — the review caught the hourly scan's boundary seam; **`20260723180000` prod-applied 2026-07-23, prod `cron.job` read-back `7,22,37,52 * * * *` active**), emailing `ALERT_EMAIL` on either §11 breach (>10 signup failures/hr; >50 login failures/hr/IP); unauthenticated invoke asserted 401 on prod. This also closes **F-2a**'s deferred alerting half. **SIGNED OFF 2026-07-23 — Closed with deferred items** ("Sign-off review 1 of 4", reviewer: claude.ai challenger chat; F-4/F-2/F-1/B-4 + G-15 all cleared; deferred: the two unwired events, B-4 observability until Sentry SDK, and silent alert-suppression under sustained-breach throttling; the constant-time EF bearer compare was deployed to prod 2026-07-23, negatives re-probed green). Review + sign-off record: `auth-onboarding-staff.md` "Sign-off review 1 of 4" + "Sign-off — Sign-off review 1 of 4". **F-1 also closed** — `organization_id_snapshot` stamped at insert (`20260723120000`), surviving org teardown. Still honestly out (structural, external-review-scoped): the two unwired events `jwt.hook_failure` + `cross_tenant_access_attempt` (no emission point exists; detection stays the G-1 probe + pgTAP 17/57 — they re-enter scope with hard-rule (a)). **Retention decision (2026-07-23):** no automated trim — auth_events is append-only indefinitely at current volume; revisit if the table approaches ~1M rows or storage pressure appears. F-3 (append-only ≠ tamper-proof against a DBA) and D-1 (UTC/AEST labelling) remain register notes, not actions.
- **Invite `action_link` minted at POST time, not send time.** ~~Deferred-with-trigger: before any paying clinical client.~~ **BUILT 2026-07-21 (staging), pending prod apply** — pulled forward as Step 4 of the internal work sequence: migration `20260721150000` (nullable `action_link`), mint moved into the gate's POST claim (OTP TTL starts at the tap; mint-failure un-claims; deploy-skew-safe legacy path), live-verified end-to-end on staging. Closure: `auth-onboarding-client.md` "C-14 deferred item 1 closure". **PROD-APPLIED 2026-07-23** (the prod-apply sitting): migration `20260721150000` applied (nullable `action_link` live in prod — types parity zero-diff); the deployed frontend now inserts `action_link: null` and mints at the gate POST. The Leg-2 bonus assertion (a fresh invite row with `action_link IS NULL` on prod) rides with the §12 Leg-2 operator probe below. Closure record: `auth-onboarding-client.md` (C-14 prod-apply appendix).
- **Enterprise Safe Links prefetch re-run.** The 2026-06-10 anti-prefetch verification covered live Gmail only; the corporate scanner class (Microsoft Safe Links, Proofpoint) is unexercised. Re-run `runbooks/verify-invite-prefetch.md` with an M365/enterprise mailbox. Deferred-with-trigger: before any paying clinical client. Owned by `auth-onboarding-client.md` (C-14 deferred item 2).
- **CN-7 — archived-client record access.** ~~today archiving a client makes their entire record UI-unreachable~~ — **BUILT 2026-07-02, pending the sign-off ritual.** The full seven-step protocol ran (new security surface → gap doc `polish/archived-client-access.md`; operator approved the contract + Q1–Q3 in-session). Shipped: the additive staff-only archived-read policy (migration `20260702190000`, pgTAP `56` 8/8 on live + 17/38/46/54 re-run green), the read-site classification + write guards (`archive-guard.ts`), the client-list Archived chip, the read-only profile with quiet banner + Restore (via the pre-existing `restore_client` RPC, humane email-conflict copy), and archive-cancels-future-appointments with reminder cascade (`soft_delete_client` v2). Deferred with re-triggers (closing commit §7): archived-profile comms history, ReportsTab deep publish affordances, the archived portal end-state, DB-level write immutability. Operator **browser verification CP-ARC-1..3 complete 2026-07-02**; full pgTAP battery (9 suites/95 assertions), scoped lint (exit 0), and live-DB residue census all green (closing commit §7). **Reviewer follow-up (§8) resolved seven items across two rounds** — six additional client-child write actions (file-delete, test capture/soft-delete/battery-tag, publish/unpublish) reachable once the profile renders for archived clients are now `assertClientLive`-guarded; role-parity and reminder-state completeness verified on live; and (round 2) the **program-builder writes** (~28 mixed-keyed actions across the session-builder differentiator) precisely enumerated and **explicitly accepted-and-named as a residual** (stale-tab race only — the program pages `notFound()` for archived; inert/reversible harm), closed with the raw-PostgREST + force-book residuals by **one DB-level write-immutability trigger at the paying-client gate**. **FM-8 (message history on archived profile) re-trigger re-labelled to the compliance boundary — before the first paying clinical client** (AHPRA/APP record-production), not a convenience trigger. These carry here as the CN-7 residuals for the paying-client gate. **CLOSED — reviewer sign-off recorded 2026-07-02 (Closed with deferred items).** The archived-read policy, guards, list chip, read-only profile + Restore, and archive-cancels-future all shipped and are browser-verified. Five residuals accepted at f&f scope and re-triggered to the paying-client gate: three write-immutability residuals (raw-PostgREST, schedule force-book, program stale-tab per FU-6) closed uniformly by one DB-level BEFORE INSERT/UPDATE trigger family (**CLOSED — prod-applied 2026-07-23, see the write-immutability entry below**); FM-8 comms-history-in-app (compliance-boundary trigger) — **CLOSED 2026-07-23 (parity pass):** the in-app MESSAGE history (distinct from the email log the Comms tab already carried) is now producible on the archived profile — additive archived-arm staff SELECT policy on `message_threads` (migration `20260723160000`, mirroring the clients-table pattern; client role deliberately gets no archived arm), the Comms tab renders the read-only transcript for archived clients (transcript **renders attachments in full** — shared `MessageAttachments`, images inline + file download chips — not a count-only marker, so the record is genuinely producible per AHPRA/APP; hardened at sign-off review 4/4, commit `3331498`), pgTAP `63` (**9/9** incl. the attachment-metadata triad) + the byte-level staging probe `scripts/verify-archived-attachment-retrieval.mjs` (3/3, real staff session fetches the blob under RLS) + the render harness cover it; closure record in `polish/archived-client-access.md`; P2-3 archived portal end-state — **CLOSED 2026-07-23 (parity pass):** the portal layout now distinguishes archived from never-onboarded (service-role probe keyed on the authed user id) and renders the designed closed-door screen (`AccessEnded` — quiet copy, sign-out, no nav) instead of dumping the archived client into `/welcome`; e2e-covered. **None gated the f&f beta** — they gated the paying-client threshold, and all five are now closed. **FM-8 + P2-3 SIGN-OFF (review 4 of 4, 2026-07-23): Closed with deferred items** (reviewer: claude.ai project chat / Opus 4.8). Reviewer returned two blocking + two record items (attachment production unverified; a staging/prod contradiction; a falsified root cause; a render/RLS predicate mismatch) — all resolved, then signed off **conditional on two read-only prod checks, both discharged the same sitting** via the prod-workdir channel: (1) **policy parity** — the two staff arms the AHPRA claim rests on (`public.message_attachments` SELECT + `storage.objects` "staff read message-attachments in own org") are byte-for-byte identical prod≡staging and migration-shaped (the storage arm is NOT dashboard-diverged — the specific thing verified rather than assumed); (2) **half-archived hunt on prod** — `(archived_at IS NULL) <> (deleted_at IS NULL)` returned zero rows. Sign-off nits: pgTAP `06` extended 13→15 asserting `soft_delete_client` sets BOTH lockstep columns / `restore_client` clears BOTH (regression cover for the render-layer fail-closed invariant); the divergent-state failure mode (transcript silently absent, `captureException` the control) accepted-and-named. Code deployed (commit `3331498`, prod health `ok`); migration `20260723160000` was prod-applied at the parity-pass sitting. Stanza in `polish/archived-client-access.md` "Sign-off — FM-8 + P2-3 closure (parity pass, 2026-07-23)". Full closing commit + sign-off in `polish/archived-client-access.md` (§7/§8/Sign-off). Provenance: `polish/client-profile-clinical-notes.md` CN-7.
- **CN-6 — `client_medical_history` last-write-wins (now-active).** ~~The table has no OCC `version` column…~~ **CLOSED 2026-07-02** — the fired trigger (two-staff beta) was acted on: migration `20260702120000` added the `version` column + `bump_version_and_touch()` trigger (replacing the plain touch trigger), and `updateMedicalConditionAction` now carries the version check; pgTAP `51_cmh_occ_version.sql` (4/4 on live) is the regression tripwire. Closure record: `polish/client-profile-clinical-notes.md` (Deferred-item closures, item 3). Residual **also closed 2026-07-02** — `client_medications` (the sibling clone) carried the identical property (org-wide UPDATE, no version), so the same §12 pattern was applied for parity rather than re-affirming acceptance: migration `20260702180000` added the `version` column + `bump_version_and_touch()` trigger, `updateMedicationAction` now carries the version check, pgTAP `55_cmed_occ_version.sql` is the tripwire, and scenario `MED-OCC-1` is the browser pass. Rationale: the two-staff clobber window is live for both tables and the fix is a trivial mirror; the short-rows argument caps blast radius but does not remove the silent-clobber failure mode on clinical data.
- **Reschedule compat-shim removal (section 7 / P0-1).** Migration `20260614140000` re-added the 1-arg `client_reschedule_program_day_to_today(uuid)` overload as a deploy-skew bridge, so prod (still calling the 1-arg form) survived the P0-1 v3 signature change to `(uuid, date)`. The shim resolves **org-tz** today and delegates to the device-tz 2-arg function — a temporary bridge that **contradicts the device-tz fix while it exists**: any call routed to the 1-arg overload silently yields org-tz, not the device-tz "today" P0-1 shipped. Harmless-ish pre-deploy (org-tz beats prod's old UTC), a latent tz-inconsistency landmine post-deploy. **Trigger: immediately after the section-7 branch merges to master and deploys** — drop the 1-arg overload in a new post-deploy migration (`DROP FUNCTION client_reschedule_program_day_to_today(uuid);`) and trim the 1-arg pgTAP `25` assertions, so every reschedule uses the 2-arg device-tz path. Do **not** push the drop before deploy (it re-breaks the still-deployed 1-arg caller). Owned by `polish/client-portal-pwa.md` (P0-1 / §8.6). **CLOSED 2026-06-14** (deploy confirmed green, then the drop applied to live): migration `20260614160000_drop_reschedule_compat_shim.sql` dropped the 1-arg overload via `supabase db push`; pgTAP `25` re-ran **20/20 on live** (the §C shim assertions removed); types regenerated (`src/types/database.ts` — reschedule now 2-arg `(uuid, date)` only, plus the P1-4 `client_log_exercise_note` RPC picked up; `tsc` green). The org-tz 1-arg fallback is gone from the catalog — every reschedule now uses the single, correct 2-arg device-tz path. Gate closed.
- **§10 P1-1(c) — client→EP email send failures are currently unobservable** ~~(best-effort `after()`, no retry, no synthetic-send assertion)~~ — **CLOSED 2026-07-02.** The queue+cron upgrade shipped exactly as prescribed, mirroring the §9 reminder posture: `message_notifications` queue + `message_notification_enqueue` DB trigger (migration `20260702140000`, RLS in `rls-policies.md` §4.29), the `send-message-notifications` Edge Function (deployed, no new secrets) drained by the Vault-token cron `message-notifications-5min` (migration `20260702150000`). The `succeeded≥1` synthetic send check **passed on live** (row `sent` + Resend `provider_message_id`) and is a standing section in `runbooks/deploy-an-edge-function.md`; pgTAP `53` (8/8) is the tripwire, pgTAP `34` re-ran 17/17. Closure record: `polish/messaging.md` §7.
- **§10 P1-2 — realtime cross-tenant isolation two-session probe.** ~~The residual is a live two-session realtime probe…~~ **CLOSED 2026-07-02 (run ahead of its trigger, with ephemeral accounts).** A live two-session probe PASSED: a staff subscriber in org B (real websocket, own JWT, `SUBSCRIBED`) received **zero** events from a foreign-org message insert over a 5-second window AND received its own org's insert on the same channel (the positive control that makes the zero meaningful, not a dead socket). Zero-residue teardown. This completes the pgTAP-34 + setup-verification chain with the live behavioural half. Closure record + probe design: `polish/messaging.md` §7.
- **§12 Part B — LOGGING HALF BUILT 2026-07-21 (staging), pending prod apply.** Step 5 of the internal work sequence shipped the brief §6.7 record: migration `20260721160000` (nullable system sender + reminder-outcome → communications trigger, incl. failed sends with reasons — the EP-facing P1-3 surfacing), app-side send logging at all four call paths, and the client-profile **Comms tab** (renders for archived clients — the FM-8 records boundary, harness-covered). pgTAP `62` (6/6) tripwire. **Render tier verified 2026-07-22** — the §5b Playwright harness was extended (seeded reminder-summary + failed row + an archived-client comms row) and runs **9/9 green** on staging, covering the summary label, the failed-row surfacing, and the archived-client Comms render. **PROD-APPLIED 2026-07-23 + Leg 1 VERIFIED on prod; Leg 2 pending the operator** (see the prod-verify sub-entry). Closure: `polish/email-and-sms.md` "Part B — the LOGGING half". **The connected-account OAuth compose half remains deferred as below.**
  - **Prod-verify — defined before the sitting (two legs).** The trigger is the load-bearing piece and it now **writes to a clinical-adjacent record**, so the teardown is defined *before* the run, not discovered after. **Leg 1 — trigger (reminder → communications):** run the standing synthetic-send check in `runbooks/deploy-an-edge-function.md` → "Synthetic send check", now §12-aware — it asserts the derived `communications` row (system send, `status='sent'`, subject "Appointment reminder") and tears it down **first** (deny-delete under RLS + FK-blocks the throwaway client, so the pre-§12 teardown FK-failed and stranded it). **Leg 2 — app-side (`logCommunication`):** the synthetic check does **not** exercise the four app-side call paths (invite, booking confirmation ×2, reschedule notification); without a named app-side probe, "prod-verified" means trigger-only and half the feature ships unverified. Probe: **invite a throwaway client** (`delivered@resend.dev`) through the app in prod → assert one `communications` row (`status='sent'`, `sender_user_id` = the acting EP, `body` = the real invite plaintext) → superuser teardown `DELETE FROM communications` for that client **first**, then the client + pending-invite/auth artifacts per `runbooks/recover-stuck-client-onboarding.md`. Both legs assert `SELECT count(*) FROM communications WHERE recipient_email='delivered@resend.dev'` = 0 afterward. The `appointment_manage_reminder` upsert re-timing pitfall is closed in the runbook (`deploy-an-edge-function.md` step 1 ⚠️, verified live 2026-06-22). **Leg 1 RAN GREEN on prod 2026-07-23:** synthetic reminder planted + re-timed → picked up by the **real production cron tick** (22:15:01 UTC — stronger than the manual curl, it proves pg_cron → Vault token → EF → Resend end-to-end) → row `sent` with `provider_message_id` → the derived `communications` row asserted exactly (`status='sent'`, `sender_user_id` NULL, `communication_type='email'`, subject "Appointment reminder") → teardown comms-row-first (the `DELETE … RETURNING` BYPASSRLS proof held) → **final census 0**. **Leg 2 RAN GREEN 2026-07-23 (operator, authed prod staff session)** — one `communications` row (`status='sent'`, `sender_user_id` = the acting EP, body carrying the real `/i/<id>` gate URL), bonus assertions confirmed mint-at-POST (`action_link IS NULL`) and the G-6 `auth.invite.sent` row, teardown census 0. **Both legs green → the §12 logging half is fully prod-verified; signed off Closed with deferred items** (`polish/email-and-sms.md` "Sign-off — Part B, the LOGGING half", incl. the reviewer's staging-only evidence caveat on the failed-branch + archived-render paths). *(This line superseded the earlier "Leg 2 has NOT run" state — caught stale 2026-07-23 during the parity pass; the sign-off in the owning doc was already recorded.)* (Historical note for automation: `.env.local`'s `PROD_SUPABASE_SERVICE_ROLE_KEY` was found dead at the sitting — see `runbooks/prod-apply-sitting-2026-07.md`.)
- **§12 Part B — connected-account email compose + Comms tab (deferred to the go-live pipeline, operator decision 2026-06-22).** The brief §6.7 requirement *"sent communications logged to the client's Comms tab"* is **not met** in section 12; the operator deferred the new-feature half of the section. Deferred scope: a **provider-abstracted "connected email account"** (OAuth send through the account holder's OWN mailbox — Gmail first, provider-agnostic — so personal compose-to-client email lands in the sender's native Sent folder + replies return to their inbox); **encrypted at-rest storage of OAuth refresh tokens** (a send-as-the-EP credential — a new sensitive secret surface that must be catalogued in `secrets-inventory.md` and join §2); **widening the `communications.provider` CHECK** (currently `resend`/`twilio` only → a connected-account INSERT fails today); and the in-app **compose modal** replacing the mail-icon `mailto:`. *(Pruned 2026-07-22 by the drift audit: the client-profile Comms tab and system-send log-wiring were originally deferred in this entry but are BUILT per the logging-half entry above — this line no longer lists them, so the two entries agree.)* Two decisions gate the build: the **FROM address** (recommend the account holder's real Gmail/Workspace address, which *sidesteps* the deferred ImprovMX "Send-mail-as" relay) and the **Google OAuth publishing / refresh-token-lifetime** choice (the 7-day testing-mode token-expiry landmine — the send path must detect a dead token and surface a reconnect prompt, never fail silently). Any new SECURITY DEFINER helper (e.g. a token resolver) joins the §4 anon-EXECUTE sweep. **Re-trigger:** the connected-account feature is picked up, **or** the operator wants a per-client communication history before then. Owned by `polish/email-and-sms.md` (Part B: P0-1/P0-2/P0-3, P1-1, P1-2, P1-4, P2-4, P2-5, §4 Q1–Q5).

- **Item 3 (block-less / one-off sessions) — deploy-ordering (2026-06-25, reviewer sign-off).** ~~Two skew hazards exist until the frontend deploys…~~ **CLOSED 2026-07-02 (satisfied by the shipped frontend).** The `is_loose`-aware frontend has long since merged and deployed — `src/app/portal/page.tsx`, `src/app/(staff)/clients/[id]/program/page.tsx` and siblings on deployed master all select and render `is_loose` — so the skew window this rider guarded (old calendar blind to loose days; copy/repeat landing on a container it can't display) no longer exists. No standalone action was ever needed beyond bundling, and the bundle shipped. Owned by `polish/one-off-sessions.md` (G3-2/G3-3/G3-5/G3-7).

- **Library Sessions + Programs editors — deferred features (2026-06-24).** Owner-scope deferrals from the in-Library editors pass (features, not hardening), checklist-tracked per the standing pattern; detail in `polish/library-sessions-programs.md` (closing commit + reviewer follow-up). (a) **Programs from-scratch authoring + week add/remove/reorder** — v1 is edit-existing-only; re-trigger: dogfooding shows abstract multi-week template design (not just editing saved blocks) is wanted. (b) **`program/new` "Start from template"** still schedules on a single start-date + the stored weekday-offset; the weekday-apply flow is the **Programs-tab** "Use template" only. Re-trigger: the operator applies from `program/new` and wants weekday selection there too — the `create_program_from_template_on_weekdays` RPC already exists, so it's a UI-only follow-up. (c) **Full `DayEditor` extraction / migrating `SessionBuilder` onto the shared editor-kit** — the hybrid's accepted clone-don't-extract trade (FM-I). Re-trigger: the cloned `DayContentEditor` drifts painfully from the builder.

- **Perf — authenticated-route Edge-middleware auth residual — RESOLVED 2026-07-21.** ~~Accepted 2026-07-01 with the Vercel-Pro re-trigger.~~ The re-trigger fired: the operator upgraded Vercel to Pro (2026-07-21, alongside the Supabase Pro cutover) and the residual closed with **no code change**, exactly as designed — prod probes show `X-Vercel-Id: syd1::syd1::…` (edge invocation AND function both in Sydney) with warm full-page TTFB ~0.18–0.29 s through the middleware. The local-JWT (`getClaims` + asymmetric keys) alternative is **moot — do not implement**; it was the no-Pro fallback and carries auth-bypass risk. Closure record: `polish/perf-middleware-residual.md` (RESOLVED section); `PERF-REGION-1` continues to guard the function-region half.

- **npm dependency-audit pass (2026-07-04) — one accepted residual: next's vendored `postcss@8.4.31`.** The 2026-07-04 triage cleared 9 advisories (1 low, 6 moderate, 2 high) down to 2 rows that are a single root cause: `next` pins its internal **build-time** `postcss` at `8.4.31` (< 8.5.10, GHSA-qx2v-qp2m-jg93 — XSS via unescaped `</style>` in stringify output). **Accepted** because the vulnerable copy only processes first-party CSS at build time (globals.css / Tailwind output — no attacker-controlled CSS reaches it), while the postcss copy that touches user-supplied content (`sanitize-html` over rich-text notes) resolves to the patched `8.5.16` — verified 2026-07-04. npm's suggested "fix" is a downgrade to `next@9.3.3` (resolver artefact, non-starter), and an `overrides` pin against a framework-internal exact dep was rejected as riskier than the advisory. What shipped that day: `npm audit fix` (dev chain: `@babel/core`→7.29.7, `brace-expansion`, `js-yaml`→4.3.0; runtime: `ws`→8.21.0 under `@supabase/realtime-js`, `resend` 6.12.2→6.17.1 which drops the vulnerable `svix`/`uuid` chain entirely) plus an explicit `next` + `eslint-config-next` **16.2.4→16.2.10** exact-pin patch bump clearing all 13 Next advisories (middleware/proxy bypass ×4, RSC cache poisoning ×2, XSS ×2, DoS ×3, SSRF, redirect cache-poisoning); `tsc --noEmit` and `next build` both green post-bump. **Re-trigger:** any future Next upgrade (next will eventually bump its own postcss pin — re-run `npm audit` then), **or** any feature that ever routes user-supplied CSS through Next's build/render pipeline.

- **Messaging attachments — first client-write storage + first client PII photo path (2026-07-13, reviewer sign-off Closed with deferred items).** Photo/file attachments shipped and CLOSED at f&f scope (`polish/messaging-attachments.md`, closing commit §7 + re-review §8/§9 + Sign-off). Two returned security findings fixed and pgTAP-locked (suite 59 23/23): (a) the stored `storage.objects` mimetype is caller-controlled — safety rests on `<img>`-only rendering, not the stored type; (b) the orphan-DELETE predicate is routed through a SECURITY DEFINER helper so archived-thread blobs stay immutable. **Boundary marker carried here so it fires, per the reviewer's out-the-door flag — this feature is the one that *manufactures* identifiable client health data (a form-check photo is exactly that):**
  - **Non-prod pgTAP target discipline.** §5 is closed (staging `odyssey-staging` exists), but the default run path — including suite 59 this session — is still live prod via `BEGIN … ROLLBACK`. Once real photos exist, **prefer the §5 staging target for any risky run on this surface**; the identifiable-health-data tripwire behind §5 is now materially closer because of this feature.
  - **Re-triggered residuals (owned by `polish/messaging-attachments.md` Sign-off):** the raw-URL user-gesture navigation / serve-`nosniff` hardening and FM-G EXIF/GPS strip (both re-fire at the first non-f&f user or the SaaS fork — this doc re-enters the polish protocol then); FM-K HEIC render quality (real report); no client-side compression (storage cost); 1-hr signed-URL expiry (user report).

- **C-1 branch-(b) / C-6 infra-error runtime verification (indexed 2026-07-22 — the drift audit's one new actionable, caught un-indexed by the reviewer in the same pass that announced the single-ledger rule).** `polish/auth-onboarding-client.md` deferred runtime verification of the C-1 branch-(b) recovery path and the C-6 infra-error branches with re-trigger "a non-production Supabase target." That trigger FIRED 2026-07-03 (§5, staging `odyssey-staging`) ~~— the verification is unblocked and has **not yet run**~~. **CLOSED 2026-07-22 — both verifications ran against staging the same day the reviewer's post-hoc verdict named them.** C-1 branch-(b) observed end-to-end: the stuck state reproduced faithfully (claimless-JWT login, then post-hoc membership + client-link injection), FinishSetup self-heal proven by the recovery sessionStorage stamp + the `/portal` render, and the poisoned-refresh failure path rendered the bounded Sign out escape, which worked (plus branch (c)'s redirect and the R-5 `/unauthorized` dead-end shape, both confirmed verbatim). C-6's opposite failure modes observed under a real induced RPC failure (`REVOKE EXECUTE … FROM authenticated`, restored + ACL-verified after): the staff gate **admitted** end-to-end with the `(failing open)` log; the accept gate **refused** with the generic message + `(failing closed)` log; the post-restore positive control re-drove the identical chain to full success, proving the revoke was the sole cause. Detail record: `polish/auth-onboarding-client.md` "Post-hoc reviewer pass discharged (2026-07-22)". Re-runnable harness: `scripts/c1-c6-staging-verify.mjs`. Rider from the same pass: pgTAP `56` extended 8→18 (the archived-client dependent-table tripwire — C-3's reading-only universal claim converted to a regression test via the real `soft_delete_client` path), green 18/18 on staging.
- **Two RLS-doc-owned deferrals surfaced by the 2026-07-22 drift audit (previously indexed only in `rls-policies.md` — a ledger hole under the single-ledger rule; both from `polish/client-profile-clinical-notes.md` Sign-off items 4/5).** (a) **`clinical_notes` client-deny automated-test coverage — CLOSED 2026-07-23 (parity pass):** pgTAP `64_clinical_notes_client_deny.sql` (staff control, `rls_clinical_notes_select_client_denied` zero-rows, client INSERT 42501, anon 42501) — `rls-policies.md` §4.6 may now be represented as test-verified. (b) **`clients.user_id` column-restriction indirection:** pre-existing bounded-harm property logged in `rls-policies.md` §4.4. **Re-trigger:** any change to the clients-table RLS policy shape. (b) remains owned by `rls-policies.md`; this entry exists so its re-trigger can fire.
- **Origin-idiom consolidation — one surviving silent-fallback origin chain (named home added 2026-07-22, per the staff-auth reviewer return).** Of the three origin-construction idioms `auth-onboarding-staff.md` Net Track B left "tracked separately": the env-chain went fail-loud (G-11, `src/lib/env/site-url.ts`) and the clients/new header-derived chain was replaced with `getPublicOrigin()` in `a81c1ca`. The survivor is the **booking placeholder chain** — `process.env.NEXT_PUBLIC_APP_URL ?? process.env.VERCEL_URL ?? ''` in the portal booking action (`src/app/portal/book/new/actions.ts`) — a silent-fallback idiom (falls through to empty string) on a portal email path, inconsistent with the platform's fail-loud posture and using the non-canonical `NEXT_PUBLIC_APP_URL` key. Low stakes (worst case: a booking email carries a broken link; both vars are set in prod and health-checked). **Fix shape:** replace with `getPublicOrigin()`; decide the canonical key (`NEXT_PUBLIC_SITE_URL`) and retire `NEXT_PUBLIC_APP_URL` or alias it. ~~**Re-trigger:** any edit to the booking actions, or the §12 Part B compose build…~~ **CLOSED 2026-07-23 (parity pass).** All app-side silent-fallback origin chains replaced with `getPublicOrigin()` — the portal booking action AND the two sibling instances in `src/app/(staff)/schedule/actions.ts` (staff booking confirmation + reschedule notification) that this entry undercounted; `EnvConfigError` joins `EmailConfigError` in each caller's config-error rethrow (fail-loud, health-checked). **Canonical-key decision recorded:** `NEXT_PUBLIC_SITE_URL` is canonical; app code no longer reads `NEXT_PUBLIC_APP_URL` anywhere — the key survives ONLY as the reminder Edge Function's link base in its own Supabase secret store, and the `required-env.ts` both-set-and-in-sync belt stays until an operator Vercel-env sitting removes the Vercel copy.
- **CN-7 write-immutability residuals + completed-session edit-lock — BUILT 2026-07-21 (staging); REVISED 2026-07-22 after reviewer return; PROD-APPLIED + VERIFIED 2026-07-23.** The paying-client-gated DB-level trigger family was pulled forward as Step 2 of the 2026-07-21 internal work sequence: migration `20260721120000_write_immutability_guards.sql` closes the three CN-7 residuals (raw-PostgREST, force-book, program stale-tab) and the completed-and-assigned lock below, uniformly, with pgTAP `60` (**17/17**, full 62-file suite green) as the tripwire. The reviewer returned the first closing commit for revision (four defects); the revision added a `clients`-row DELETE guard, narrowed the `archive_cascade` GUC to a single guard (+ GUC-forgery tripwires), kept `search_path = public, pg_temp` on the guards (the round-1 bare-`public` was reverted), and **explicitly accepted the day-row unassign bypass** (completed-lock entry below). Contract + revised closing commit + reviewer-return log + sign-off (Closed with deferred items 2026-07-22): `polish/db-write-immutability.md` §7/§8/Sign-off. **PROD APPLY DONE 2026-07-23** (the prod-apply sitting): migration applied via the prod-workdir channel; the sign-off's light-smoke precondition ran green on prod — 9 guard triggers in the catalog, CR-normalised function-body parity vs staging on all seven guard/RPC functions, and a rolled-back live refusal probe (archived edit + archived hard-delete both refused with the exact app copy, under `test_enforce_guards='1'`). The prod exposure window is **CLOSED** — `incident-response.md` §10 (closure entry, incl. the amendment of its stale "pgTAP 60 on prod" close-condition to the sign-off's light smoke). Prod-apply record: `polish/db-write-immutability.md` (prod-apply appendix).
- **Completed-session edit-lock — DB-level enforcement BUILT 2026-07-21 (staging), PROD-APPLIED 2026-07-23 (rode the write-immutability apply above); one residual accepted 2026-07-22.** The DB half now ships in the write-immutability trigger family above — `program_write_guard` refuses writes to `program_exercises`/`program_exercise_sets` under a completed+assigned day (pgTAP 60 #7/#8 refuse, #9 assigned-not-completed control, #10 unassign unlock) — and `program_write_guard` is confirmed present on prod with body parity vs staging (2026-07-23). The paragraph's UI-guardrail description below is retained as history; **the "not DB-refused" clause it carries is no longer the prod state** — the DB refusal is live as of 2026-07-23. ~~**Accepted residual (reviewer 2026-07-22, blocker 1):** unassign (`published_at → NULL`) is a raw `program_days` UPDATE and stays the sanctioned unlock at every layer… Re-trigger for the hard gate (RPC-only unassign): before any paying clinical client, or a non-trusted second staffer.~~ **HARD GATE CLOSED 2026-07-23 (parity pass):** migration `20260723140000` — `program_write_guard` v2 branch (c) refuses a raw `published_at → NULL` transition on a day with a completed live session unless the transaction-local `odyssey.day_unassign` GUC is set, and `unassign_program_day(uuid)` (SECURITY DEFINER, org/role-guarded, the GUC's only setter) is now the app's single unassign path (`unpublishProgramDayAction` routes through it for all days). Unassigning a NOT-completed day stays permitted raw (nothing is locked) — the app uses the RPC uniformly anyway. pgTAP `60` #18–25 (control, refusal, RPC success, verify, client-role 42501, GUC-disarm post-RPC refusal, cross-org definer refusal, GUC-disarm no-op path). **SIGNED OFF 2026-07-23 — Closed with deferred items** (reviewer: claude.ai project chat, sign-off review 3 of 4; stanza in `polish/db-write-immutability.md` "Sign-off — completed-lock HARD GATE closure"). Round 1 closed both blockers: `20260723190000` disarms the GUC immediately after the RPC's UPDATE (B1 — the arm no longer outlives its one sanctioned statement; #23, and #25 for the idempotent-no-op primitive B1 named), and the SECURITY DEFINER own-org check is now asserted in code (B2 — #24). Deploy order: DB first, always (`unassign_program_day` must exist before the frontend that calls it). **Carry condition 1 (open):** prod goes to RPC v2 with the both-hashes read-back (guard `abe4dae2…`, RPC v2 `2ddf6690…`) at the prod-apply sitting — the sign-off does not hold until prod parity is confirmed. **Named residual (deferred):** a `published_at` non-null→non-null rewrite on a completed day is permitted and unasserted — does not unlock the prescription, but mutates the published-at audit-trail value; re-trigger: before identifiable client health data enters. Deploy-skew note: old-frontend raw unassign of a LOCKED day fails with the guard's clear message during the minutes between DB apply and frontend deploy — accepted. The original UI-guardrail context (deferred 2026-07-15) is unchanged and follows. The session builder renders **read-only** when the client has completed a session and it is still assigned (`SessionLockContext` in `SessionBuilder.tsx`; `locked` computed in the day page from a `sessions.completed_at` probe; the program calendar shows a matching green "Completed" tick). This is a **UI guardrail only** — every write control is gated in the component and the mutation handlers early-return, but the underlying server actions / raw PostgREST writes to `program_exercises` + `program_exercise_sets` are **not** DB-refused for a completed day. The only actor who can bypass the UI lock is the EP, via raw API, on their **own** org's data (RLS still enforces tenant isolation) — no cross-tenant / privilege surface, so accepted at f&f scope. Same class as the CN-7 program-builder write-immutability residual above (stale-tab / raw-PostgREST), and a completed prescription is a clinical record. **Re-trigger:** before the first paying clinical client — fold into the CN-7 DB-level write-immutability trigger family at that gate — **or** if any completed session is ever found edited after the fact. Shape when picked up: a BEFORE UPDATE/DELETE trigger (or RPC guard) refusing writes to `program_exercises`/`program_exercise_sets` whose parent day has a completed, still-assigned session, plus a pgTAP tripwire. Owned by this checklist (dogfooding change within the signed-off calendar/builder surfaces — no polish doc re-open; provenance `docs/polish/program-calendar.md` §10).

- **Guard-bypass GUC residual (indexed 2026-07-23; widened same day).** From the write-immutability Sign-off's deferred list (`polish/db-write-immutability.md`): the guards whose bypass safety rests on the PostgREST boundary (no API surface can set a caller-named GUC today; the standing check is "no caller-supplied `set_config` first arg" in any new RPC). **Two GUCs now in this class:** `odyssey.archive_cascade` (consulted only by `clients_row_write_guard`; set only by `restore_client`) and `odyssey.day_unassign` (added `20260723140000`; consulted only by `program_write_guard` branch (c); set only by `unassign_program_day`, and since `20260723190000` disarmed again before the RPC returns — the arm is statement-scoped, not transaction-scoped). GUC-forgery tripwires exist in pgTAP `60` (incl. #23 post-RPC disarm). **Re-trigger:** any API-exposed GUC setter, or the SaaS fork.
- **`reminder_log_communication` trigger-exception → UNBOUNDED reminder resend (severity RAISED 2026-07-23 from "duplicate-send" per the Package-1 gating review; latent, not reachable today).** *Root cause, corrected — it is NOT the SMS branch specifically.* `reminder_log_communication` is an AFTER UPDATE trigger on `appointment_reminders`; **any** exception it raises aborts the whole UPDATE statement, rolling back the Edge Function's terminal status write. `markSent`/`markFailed` then never commit, the row stays `status='scheduled'`, and the next 5-minute cron tick re-selects it (the EF's due-row SELECT filters only `status='scheduled' AND provider='resend' AND scheduled_for<=now`). **Retry does NOT bound this** (investigation, 2026-07-23): the `retry_count`/`MAX_RETRIES=5` ceiling guards only the transient-send-failure path (`markRetry`, which keeps `status='scheduled'` so the trigger's guard early-returns — trigger-safe); **neither `markSent` nor `markFailed` increments `retry_count`, and the SELECT has no `retry_count` ceiling**, so a trigger exception on the **sent** path is an *unbounded real re-send* — the client is emailed every tick until the appointment is cancelled or a human intervenes — not a bounded duplicate. (No `provider_message_id` dedupe and no cron-overlap guard exist either; the only idempotency is the `WHERE status='scheduled'` on the terminal write, which does not help once that write keeps rolling back.) The concrete reachable instance is the **SMS branch** (`reminder_type LIKE '%sms%'` → `communication_type='sms'` with no `recipient_phone`, violating `communications_recipient_matches_type`), unreachable today because the lifecycle only enqueues `reminder_24h_email`. ~~**Fix shapes when picked up:** make the trigger's INSERT non-fatal to the reminder write; and/or fix the SMS branch before activating SMS; and/or add a retry ceiling so no row can be reprocessed unboundedly.~~ **CLOSED 2026-07-23 (parity pass) — all three fix shapes shipped.** (1) Migration `20260723130000`: the derived-log INSERT runs in its own exception scope — a logging failure can no longer abort the reminder's terminal status write (the root cause is dead; the miss is `RAISE WARNING`'d). (2) The SMS branch is constraint-valid: `recipient_phone` carried from `clients.phone` (a phoneless client's SMS log lands in the exception scope, not an abort). (3) The Edge Function is send-bounded belt-and-braces: a failed terminal write is now observable (previously silently discarded; **logged — EF/Postgres logs — not alerting**) and bumps `retry_count` in a trigger-safe statement, and a row at the ceiling is terminally failed WITHOUT another send. **Bound stated honestly (corrected at the 2026-07-23 sign-off review):** ≤ `MAX_RETRIES` (5) real emails per row whenever the terminal-write failure doesn't also kill the bump; **unbounded remains possible under correlated failure** (EF loses the DB or dies between send and bump — rare, requires the infra failure to recur every tick). Structural fix for that residual (pre-send attempt increment, or a Resend `Idempotency-Key` — provider behaviour verified: 24 h retention on `POST /emails`, keyed `reminder/<id>/<retry_count>`; **precondition: payload byte-stable across ticks — same-key/different-payload is a 409, so the body must stay tick-relative-content-free, which it currently is**) is **deferred**. **Re-trigger — passive/user-reported today, mandatory at the next gate:** the only current signal is a client reporting a duplicate email (nothing detects it — surfacing is logged, not alerting); acceptable **only** at f&f scope, and **the structural fix becomes mandatory at SMS activation or the first paying clinical client**. The EF's next functional touch is the ride-along opportunity. **Repo-vs-deployed note:** the deployed EF lags the repo by the comment-only bound-claim correction (behaviour identical) — a comment delta in a prod-vs-repo diff is expected; a code delta is real drift. Deliberate tightening: the legit transient path's total attempts go 6 → 5. Tripwires: pgTAP `62` #7–9 (non-fatal commit, no-row-derived, valid-sms-row) for the trigger; `scripts/reminder-logic-verify.mjs` #13–17 (extended 12 → 17 at the sign-off review) for the EF send bound, incl. #17 locking the correlated-failure residual in as expected. Full closure record + review corrections: `polish/email-and-sms.md` (Unbounded-resend closure section). **Standing note (unchanged): any change to the `communications` table's constraints/columns/grants should re-run pgTAP 62** — the non-fatal scope now caps the blast radius at a lost log row, not a resend loop. Related operational note (same review): service-role hard-DELETE of an *archived* `clients` row is refused by design — org-cleanup scripts (the EXCO pattern) must restore-then-delete or run as postgres.
- **`communications.sender_user_id` nullability — equality-predicate audit (2026-07-23, Package-1 gating review).** Making `sender_user_id` nullable (NULL = system send) means any RLS predicate, WHERE clause, join, or aggregate that compares it for **equality** silently excludes system-send rows (`NULL = x` is NULL, not TRUE — filtered, never raised). **Audit result:** the `communications` **SELECT** policy is clean — `organization_id = user_organization_id() AND deleted_at IS NULL AND user_role() IN ('owner','staff')`, no `sender_user_id` predicate — so system rows are visible on the Comms tab (confirmed: the Leg-1/Leg-2 prod rows rendered). The **INSERT** policy DOES carry the predicate — `WITH CHECK (… AND sender_user_id = auth.uid())` — so a NULL-sender INSERT via the **authenticated** role is silently rejected; this is why system sends MUST go through the service-role client (`src/lib/comms/log.ts`) or the SECURITY DEFINER trigger (both bypass RLS). Not a bug today (no authenticated path inserts a system send), but the live instance of the trap for any **future** consumer. **Re-trigger: any new consumer of the `communications` table** — Delphi analytics named explicitly — must be checked for `sender_user_id` equality predicates that would drop system sends.
- **Reminder `communications` rows store a summary line, not the verbatim body (2026-07-23, Package-1 gating review).** `reminder_log_communication` writes a factual summary (`'Appointment reminder for <type> on <date>.'`), not the exact email that shipped (the Edge Function's rendered HTML/text is not captured); the Comms tab labels these rows as summaries. Consequence: once the reminder template changes, historical reminder content is **not reconstructible** from the record. Accepted at f&f scale (the summary is an accurate factual record of what the reminder concerned). **Re-trigger: identifiable client health data entering the project** — the same boundary as the standing non-prod-pgTAP liability (`polish/messaging-attachments.md`) — at which point decide whether to capture the verbatim rendered body.
- **R-4 request-path half — real-token PostgREST cross-tenant denial (indexed 2026-07-23; previously tracked nowhere). BUILT + RUN GREEN the same day (parity pass).** pgTAP `17`/`57` prove RLS isolation under JWT-spoofed sessions inside the DB; the C-1/C-6 staging harness proved app-path behaviour; **`scripts/r4-request-path-verify.mjs` now exercises the remaining leg** — real GoTrue-issued tokens for both seeded orgs driving raw PostgREST over HTTP (gateway → PostgREST → RLS): cross-read by id, cross-list by org, cross-PATCH (0 rows with representation), cross-INSERT (42501), plus own-org positive controls and a same-org PATCH control so a zero can never be a dead endpoint. Staging-only by refusal; cred-desync self-heal built in. **Standing:** re-run on any change to the JWT custom-claims hook; it remains the named test case handed to the external IT reviewer (hard rule (a)), who should re-run it against production with their own credentials.

- **Staging-to-prod schema parity over time (standing liability; indexed 2026-07-23 — was named only inside the G-6 register's Q-1 note in `auth-onboarding-staff.md`, where its re-trigger could never fire).** The permanent staging project makes every verification's validity conditional on staging and prod holding the same schema/config. Standing controls: migrations apply to both through the same files; `migration list` on both targets at every prod-apply sitting (zero-pending on both = parity); the types-parity check (`npm run supabase:types` against staging → zero diff against the committed types the prod app runs) at every deploy sitting; auth-config parity re-checked via `verify-auth-config.mjs` both-targets on any auth-config change. Known permanent deltas, accepted: staging carries the pgTAP `00_test_helpers` posture and synthetic seed; cron jobs on staging point at prod EF URLs by pattern (ticks 401 harmlessly); Edge Function *deployments* are prod-only. **Re-trigger for a full drift audit:** any dashboard-side (non-migration) schema or config change, or a verification that passes on staging and fails on prod.
- **2026-07-23 paying-client parity pass — exclusions register (what remains open at the paying-client gate, and why).** The parity pass closed every §8 paying-client-gated *build* item. What remains is exactly the set that needs money, an external party, or an operator-only action — recorded here so none of it reads as forgotten: **(1) PITR** — paid add-on; deferred posture unchanged (`slos.md` §2.2); *enable at first paying client*. **(2) Weekly S3 exports + monthly restore-drill cadence** — paying-client backup posture (`slos.md`); needs paid storage; *establish at first paying client*. **(3) Enterprise Safe Links prefetch re-run** — needs an M365/enterprise mailbox that does not exist in the project; entry above stands. **(4) Pen test** — explicit deferred-with-justification decision (2026-07-23) for the Tier-2 boundary: costs money and requires an external party; it rides hard rule (a)'s external review scope. Recording this decision satisfies the Tier-2 "not deciding is not an option" clause ahead of time. **(5)** ~~§12 Leg-2 prod probe~~ — already DONE (operator, 2026-07-23; see the §12 entry above) — struck from this register. **(6) Client session-duration revisit** — a config decision contingent on real paying-client device profiles (shared devices → shorter sessions); operator dashboard action at onboarding. **(7) §12 Part B connected-account compose** — a *feature* deferral (operator decision 2026-06-22) gated on two operator OAuth decisions; not hardening; entry above stands. **(8) External review / BAA / entity** — hard rule (a)/(b)/(c), excluded by definition. **(9) Cliniko data migration** — its own project per the roadmap, operator-owned data export.

## Closing note

This checklist is an index, not a vault. When an item closes, record the closure in the document that owns the item, and mark the gate here closed with a pointer to that record. Do not migrate the detail into this file.
