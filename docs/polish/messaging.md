# Polish-pass gap analysis — Messaging (section 10)

**Brief:** `Client_Platform_Brief_v2.1.docx` is **silent-by-exclusion** on this section. §6.7, §10 ("What NOT to Build"), and the practitioner-context table all state, explicitly and repeatedly, **"No in-app messaging or chat (email is the communication channel)."** In-app messaging is therefore an **owner-approved deviation from the brief**, not a brief deliverable — recorded in the schema itself (`20260425100000_messages.sql:6-9`: *"Adopted in deviation from brief §6.7 … after explicit owner approval — kept inside the existing RLS/audit perimeter rather than a third-party chat SDK so health-adjacent message content stays subject to APP compliance + DR tooling."*). The governing intent for this pass is **CLAUDE.md polish-pass order item 10** — *"in-app messaging between staff and client portal. Texting-style feel, privacy preserved"* — read together with the **guardrails the deviation's approval was predicated on** (see §0, §1). Constrained throughout by §2.5 (Ownership & Privacy), §7 (compliance/retention/audit), and the design system + voice.
**Reference prototype:** none. No messaging prototype exists among the four root `.html` files (calendar, session builder, client portal, dashboard). Where intent is silent, the established texting-style shipped behaviour and the design system are the reference.
**Carried-in rider this section OWNS** (from CLAUDE.md active-section line + [`docs/go-live-checklist.md`](../go-live-checklist.md) §4, lines 72 & 74): the **§10 slice of the platform-wide anon-EXECUTE sweep** — `client_cascade_thread_archive()` ("no-arg; confirm it is a trigger/internal helper, not caller-facing, then revoke anon + authenticated"), plus its sibling `message_update_thread_last()` surfaced during this audit.
**Current implementation:** DB — [`20260425100000_messages.sql`](../../supabase/migrations/20260425100000_messages.sql) (tables, RLS, realtime, the bump trigger), [`20260426110000_message_threads_cascade_client_archive.sql`](../../supabase/migrations/20260426110000_message_threads_cascade_client_archive.sql) (archive cascade). Staff — [`messages/page.tsx`](../../src/app/(staff)/messages/page.tsx), [`Inbox.tsx`](../../src/app/(staff)/messages/_components/Inbox.tsx), [`messages/actions.ts`](../../src/app/(staff)/messages/actions.ts). Portal — [`portal/messages/page.tsx`](../../src/app/portal/messages/page.tsx), [`ClientThread.tsx`](../../src/app/portal/messages/_components/ClientThread.tsx), [`portal/messages/actions.ts`](../../src/app/portal/messages/actions.ts). Shared — [`src/lib/messages/types.ts`](../../src/lib/messages/types.ts). Nav unread badges — `TopBar.tsx`, `BottomNav.tsx`. (Distinct from `communications` — the email/SMS log, `20260420102100` — which is **not** in this section's scope.)
**Audit date:** 2026-06-18
**Status:** **Gap list produced (protocol steps 1–4 complete). Awaiting approval of the gap list (step 5) before any code lands. No code has been changed.** The gap list (§3) is the contract.

---

## 0. Executive summary

The messaging subsystem is **already built and, for its happy path, genuinely well-made** — it was landed in late April 2026 (migrations `20260420`–`20260426`) well before the polish pass reached it. The protocol's caution applies here in full: *the existing code is largely correct, and this pass must not assume otherwise.* What works, works cleanly:

- **A sound data model.** One thread per `(organization_id, client_id)` with denormalised `last_message_*` for fast inbox rendering; `messages` rows with `sender_role` **captured at insert** (survives a staff demotion), an `organization_id` denormalised onto the hot table for join-free RLS, and a 1–1000-char body cap (`messages.sql:77`) deliberately keeping this lane for *short coordination, not clinical narrative*.
- **Both surfaces, fully wired.** A staff inbox (thread list, search, conversation pane, client snapshot side panel, quick replies, optimistic send with realtime canonicalisation) and a mobile portal thread (same realtime/optimistic core). Unread badges live-update in both navs via realtime.
- **Cross-tenant RLS present** on both tables for both roles, **hard DELETE denied** (soft-delete only — retention-aware per §7.2), the archive-cascade keeps a thread's `deleted_at` in lockstep with its client, and a `enforce_same_org_fk` trigger guards the thread→org and message→thread→org invariants.
- **A real clinical-safety instinct already in the UI** — a *"For urgent issues, call 000"* banner on the staff side panel (`Inbox.tsx:600-603`) and the portal (`ClientThread.tsx:217,318`), plus *"Reply within business hours."*

So this is **not** a build. The work is concentrated in three places, none of them the happy path:

1. **The compliance/security perimeter — the condition the owner's approval was predicated on.** The deviation was approved *because* messaging would stay "inside the existing RLS/audit perimeter … subject to APP compliance + DR tooling." Two halves of that condition are currently **unmet**: (a) the migration header claims messages are *"immutable except `read_at`/`deleted_at` — enforced via update RLS,"* but the actual UPDATE policies impose **no column-level restriction**, so within a thread a client can edit or soft-delete the **EP's** messages and forge attribution (FM-1); and (b) `messages`/`message_threads` carry **no audit trigger** and are **absent from `audit_resolve_org_id()`**, so that tampering is also **untraceable** (FM-2), against §7.4. Add the named anon-EXECUTE rider (FM-3) and the **complete absence of RLS test coverage** on a brand-new tenant surface (FM-4), and the four P0s are all perimeter, not features.

2. **The "texting-style feel" gap.** Texting *notifies*. There is **no out-of-app notification** of any kind — no email-to-EP on a new client message, no web push (FM-5). A friends-and-family tester who messages the EP gets silence until the EP next opens the inbox; the same in reverse. The unread badge only moves while the app is already open.

3. **A short tail of polish** — clinical-safety disclosure strength, a token sweep, compose-flow friction, and the external-review doc gap.

The audit method: deep reads of every messaging file (DB, both server actions, both client components, both navs) cross-checked against the audit-resolver coverage guard, the pgTAP inventory, and the go-live rider text. The two security findings (FM-1 immutability, FM-2 audit) were each verified twice — once against the creating migration, once against the resolver CASE list and the full migration set (`grep` for any later audit-attach on messages → none).

### 0.1 Sign-off log (2026-06-18)

Operator answered all five questions; recorded here. The notification scope (Q1 + Q5) resolved **larger** than the original draft — web push is pulled in from deferral, and an in-app home-screen indicator is added. The one sub-decision left to recommendation is the **email fallback** (P1-1(c)); it proceeds as recommended unless overridden. The gap list still awaits final approval of the whole contract before code lands. **No code has been changed.**

| Q | Decision | Notes |
|---|---|---|
| **Q1** | **In-app notification indicator, top-right on the home screen.** | A prominent new-message indicator (unread count + state) top-right on the home surface — the staff `TopBar` bell (already top-right, global incl. the dashboard) and the client portal home (`PortalTop`). Always accurate while the app is open. → **P1-1(a)**. |
| **Q2** | **(a) `BEFORE UPDATE` trigger.** | Rejects any change except `read_at`; forbids a client mutating a `sender_role='staff'` row. → **P0-2**. |
| **Q3** | **(a) No delete / unsend in v1.** | `deleted_at` is not a user-mutable field; the only soft-delete path stays the SECURITY DEFINER thread archive-cascade. Messages are write-once + mark-read. → tightens **P0-2** (the trigger blocks `deleted_at` too). |
| **Q4** | **(a) First-open one-liner + persistent footer.** | A once-dismissed "not for urgent/clinical — call 000 / book" line on first open, plus the existing footer. → **P2-1**. |
| **Q5** | **Pull web push INTO scope, viability-gated.** | Real push if viable. Viable for desktop staff + installed-PWA clients; **iOS delivers push only to a PWA installed to the home screen (iOS 16.4+)** — so push is paired with the email fallback, not a replacement for it. → **P1-1(b)**, behind a viability spike. |

**Notification stack (resolved shape — P1-1).** Three layers, because no single one covers every case:
- **(a) In-app indicator (Q1)** — top-right home-screen; always works when the app is open. Definite, cheapest, ships first.
- **(b) Web push (Q5)** — best out-of-app UX (a real ping when the app is closed); reaches desktop staff + installed-PWA clients; **does not reach un-installed iOS clients**. Built behind a viability spike.
- **(c) Email fallback (recommended)** — email-to-EP on a new `client→staff` message, reusing the verified Resend path. Guarantees the **EP** never misses a client message even when push isn't set up or can't reach (the iOS gap) — the worst-case failure (a client waiting on a reply). EP-direction only; EP-reply→client stays push + in-app (per-reply email would be spammy). Proceeds as recommended unless overridden.

---

## 1. Intent conformance — CLAUDE.md §10 + the owner-approved deviation's guardrails

There is no brief section to conform to line-by-line (the brief *excludes* messaging). Conformance is assessed against CLAUDE.md §10's stated intent and the guardrails the deviation's approval was conditioned on. "Met with perimeter gap" means the surface works but a named security/compliance gap below tightens it.

| Intent / guardrail | Status | Evidence / provenance |
|---|---|---|
| **CLAUDE.md §10** In-app messaging between staff and client portal | **Met** | Staff inbox + portal thread both exist and are nav-surfaced; two-way send works end-to-end over RLS-scoped tables. |
| **"Texting-style feel"** | **Mostly met** | Bubbles, day dividers, relative timestamps (deck slide-05 conventions), realtime stream, optimistic send with realtime de-dupe, quick replies (`Inbox.tsx`). **Gap:** texting notifies; there is **no out-of-app notification** (no email/push) — the unread badge only moves in-app. → **P1-1** |
| **"Privacy preserved"** — cross-tenant isolation | **Met (untested)** | Org + client-scoped RLS on both tables for both roles (`messages.sql:154-257`); `enforce_same_org_fk` triggers; hard DELETE denied. **But:** the new surface has **zero pgTAP coverage** — isolation is asserted, never verified. → **P0-4** |
| **"Privacy preserved"** — within-thread record integrity | **Not met** | Migration header (`messages.sql:15-16`) + table COMMENT (`:108-109`) claim immutability *"enforced via update RLS."* The UPDATE policies (`:211-215` staff, `:245-253` client) check only org/role/thread — **no column restriction** — so a client can edit/soft-delete the **EP's** messages and forge `sender_*` in their own thread; staff can rewrite any message in the org. The claimed invariant is unenforced. → **P0-2** |
| **Deviation guardrail** "subject to APP compliance + DR tooling" — **audit** (§7.4) | **Not met** | `messages`/`message_threads` carry only `touch_updated_at` / `enforce_*_org` / `bump_thread_last` triggers — **no audit trigger** — and are **absent** from `audit_resolve_org_id()`'s CASE list (only `communications` is registered; `20260513160000:96-101`). All data access/modification on the new clinical surface is **unlogged**. → **P0-3** |
| **Deviation guardrail** "subject to APP compliance + DR tooling" — **retention** (§7.2) | **Met** | Soft-delete only (hard DELETE denied both tables); archive-cascade syncs `deleted_at`; no purge path. 7-year retention posture inherited. |
| **Deviation guardrail** "short coordination, not clinical narrative" | **Met** | 1–1000-char DB CHECK (`messages.sql:77`) + UI counter + the 000 / "book an appointment" banners steer away from clinical use. Disclosure could be stronger (→ **P2-1**) but the lane is correctly framed. |
| **CLAUDE.md** "anon-EXECUTE sweep … `client_cascade_thread_archive` (§10)" | **Not met (low real risk)** | `client_cascade_thread_archive()` and `message_update_thread_last()` are SECURITY DEFINER with the Supabase default anon/authenticated grant. Both are **trigger-only** (not PostgREST-invocable) so exploitability is ~nil, but the revoke + tripwire is the named rider. → **P0-1** |
| **Realtime** "postgres_changes respects RLS" (`messages.sql:18-20,141-145`) | **Unverified** | Plausibly true (RLS is enabled; default behaviour) but never probed on live for this multi-tenant stream. → **P1-2** |
| **Design system** tokens / voice | **Mostly met** | Uses `var(--…)` widely; some inline raw px/rem/opacity literals remain, and a few hard-coded strings ("Reply within business hours") + "say hello" empty states drift from the quiet-clinical voice. → **P2-3 / P2-4** |
| **§10 "What NOT to build"** posture — attachments | **Deferred (consciously)** | Paperclip button is present-but-`disabled` with "coming in a later phase" (`Inbox.tsx:521-529`). Accepted. |
| **Architecture docs** coverage (Open-gates external review) | **Partial** | `schema.md` references the tables; `rls-policies.md` (the doc the external IT review reads) does **not** carry the messaging policies. → **P2-5** |

---

## 2. Premortem — ranked failure modes (protocol step 3)

Weighting per protocol: **infrastructure and security at production grade**; **operational, UX, and workflow at friends-and-family scope** (operator + one EP collaborator + small invited circle, no paying clinical clients). A gap closing a high-likelihood failure mode is promoted in priority. Messaging is weighted more like a production surface than most f&f UX, because freeform messages are **the most likely place clinically-relevant content lands** outside the clinical-notes table — which makes the compliance perimeter load-bearing.

| # | Failure mode | Likelihood / exploitability | Impact | → |
|---|---|---|---|---|
| **FM-1** | **Within-thread message tampering — claimed immutability is unenforced.** `messages.sql:15-16` asserts messages are immutable except `read_at`/`deleted_at`, "enforced via update RLS." They are not. Client UPDATE policy (`:245-253`): `USING (role=client AND own thread)`, `WITH CHECK (role=client)` — **no column restriction**, so a client can `UPDATE` the **body** of the EP's messages, set `deleted_at` on them (soft-delete the clinician's words out of both views), or forge `sender_role`/`sender_user_id` on any row in their own thread, via a direct PostgREST `UPDATE` (not reachable through the UI, but the REST endpoint is open to the client's own JWT). Staff UPDATE (`:211-215`): `WITH CHECK (org)` only — any staff can rewrite any message in the org. Org/thread scoping holds (no cross-tenant leak), but within-thread clinical-record integrity is unprotected. | Certain (policies confirmed) / Low via UI, **High via raw REST** | Clinical-record integrity on a health-adjacent surface; **the exact condition the owner's approval was predicated on** | **P0-2** |
| **FM-2** | **No audit trail on the messaging surface.** `messages` + `message_threads` have no audit trigger and are absent from `audit_resolve_org_id()` (`20260513160000:96-101` lists `communications`, not these; a full-migration grep finds no later audit-attach). §7.4 requires "audit logging for all data access and modifications," and the deviation's approval was explicitly conditioned on APP-compliance + DR tooling. The FM-1 tampering is therefore **both possible and undetectable**. | Certain | §7.4 / APP-compliance gap on the new clinical surface; the unmet half of the approval condition; no DR/forensic trail | **P0-3** |
| **FM-3** | **Messaging SECURITY DEFINER functions retain the anon grant (named §10 rider).** `client_cascade_thread_archive()` and `message_update_thread_last()` are SECURITY DEFINER and carry the Supabase default anon/authenticated EXECUTE. Both are **trigger-only** (`RETURNS TRIGGER`, depend on `TG`/`NEW`/`OLD`) → not invocable through PostgREST, so real exploitability is ~nil — but this is the **explicitly-named** go-live rider (`go-live-checklist.md:72,74`) and the regression-tripwire pattern (sections 5/6/7/9) applies. | Certain (grant present) / ~Nil (not callable) | Defence-in-depth + named rider; posture consistency with the platform-wide sweep | **P0-1** |
| **FM-4** | **Zero RLS test coverage on a brand-new tenant surface.** pgTAP `01`–`33` include **no** `messages`/`message_threads` test. Cross-tenant isolation — the #1 multi-tenant failure mode per CLAUDE.md Open gates and an explicit execution gate (`go-live-checklist.md` §6) — is **unverified** for messaging, and the FM-1 immutability fix (P0-2) and FM-3 grants (P0-1) would ship with no tripwire. | Certain (no tests) | The isolation gate is unmet for the section; no regression guard on the other P0s | **P0-4** |
| **FM-5** | **Messages can sit unseen indefinitely — no out-of-app notification.** The unread badge updates only via in-app realtime or on next page load (`TopBar.tsx`, `BottomNav.tsx`). There is no email-to-EP on a new client message and no web push. A solo EP does not live in the inbox; a client message can sit for hours/days, and an EP reply the same in reverse. "Texting-style feel" implies the opposite. | High (the EP does not live in-app) | Feature reads as broken/ignored; **compounds FM-7** when the message is urgent | **P1-1** |
| **FM-6** | **Realtime RLS enforcement unverified on live.** Both surfaces subscribe to `postgres_changes` on `messages` filtered by `thread_id`/`organization_id`; the migration asserts RLS gates delivery. Supabase respects RLS on `postgres_changes` by default when RLS is enabled (it is) and the subscriber uses its own JWT — but this is **unverified on the live project** for this multi-tenant clinical stream, and a misconfiguration streams rows cross-thread/cross-tenant. | Low (default is safe) / High if misconfigured | A realtime RLS bypass is a **cross-tenant leak** — production-weighted | **P1-2** |
| **FM-7** | **A client uses messaging for an urgent clinical issue and it is not seen in time.** Freeform messaging invites "knee gave way, sharp pain" / "chest pain since the session." Combined with FM-5 (no notification), an urgent message can sit unseen. The mitigation is **already present and load-bearing** — the 000 banner on both surfaces (`Inbox.tsx:600-603`, `ClientThread.tsx:217,318`) + "Reply within business hours" — but it is a small persistent footer, not a first-open disclosure. | Medium / High (clinical) **but mitigated** | Clinical-safety; the primary safety net exists, residual is disclosure strength + the FM-5 gap | **P2-1** (copy) + **P1-1** |
| **FM-8** | **Thread restore resurrects an intentionally-archived thread.** `client_cascade_thread_archive()` un-archives the thread whenever the parent client is restored (`20260426110000:40-46`, comment: "we don't try to distinguish"). No thread-level archive UI exists today, so this is latent — but if one is ever added, restoring a client silently revives a thread the EP closed. | Low (no separate thread-archive path today) | Latent surprise if thread-level archive is added later | **P2-2** / accept |
| **FM-9** | **Design-token / inline-literal drift.** Both components carry inline `style={{…}}` with raw px/rem/opacity literals alongside proper `var(--…)` usage — the same code-standard class as scheduling P2-13. | Certain / cosmetic | Token-discipline erosion; divergence on any palette change | **P2-3** |
| **FM-10** | **Compose friction + hard-coded copy + voice drift.** Staff "New message" links to the whole `/clients` list rather than a pick-client composer (`messages/page.tsx:93-95`); "Reply within business hours" and the quick-reply set are hard-coded; empty states say "say hello," slightly off the quiet-clinical voice. | Medium / low | Minor workflow friction + voice drift | **P2-4** |
| **FM-11** | **Messaging RLS undocumented for the external-review gate.** `schema.md` references the tables; `rls-policies.md` — the doc the Open-gates external IT review reads — does not document the messaging policies. The highest-impact multi-tenant surface would go to review undocumented. | Certain (absent) | Open-gates external-review readiness | **P2-5** |

### Accepted rather than mitigated (with rationale and re-trigger)

- **In-app messaging itself is an owner-approved deviation from brief §6.7.** Not re-litigated in this pass — documented in §0 and conformed against CLAUDE.md §10 + the approval guardrails instead. **Re-trigger:** the owner withdraws approval, or the brief is revised.
- **Attachments stay stubbed** (disabled paperclip, "coming in a later phase", `Inbox.tsx:521-529`) — matches the §10 "What NOT to build" posture. **Re-trigger:** a beta tester needs to send a photo (e.g. an exercise form-check).
- ~~**Web push deferred**~~ **No longer deferred — pulled into scope as P1-1(b)** (operator Q5, 2026-06-18). Built behind a viability spike and paired with the email fallback to cover the iOS-un-installed gap.
- **One thread per client, org-shared (any staff can read/reply).** Correct for the multi-practitioner-ready architecture (a shared practice inbox); single EP today. Accepted (architected, not a gap).
- **No edit/unsend in v1.** Locking messages immutable (P0-2) is the deliberate choice for a clinical record; texting-style edit/unsend is revisited post-beta. **Re-trigger:** operator answers Q3 (b)/(c), or beta feedback asks for unsend.

---

## 3. Gap list (protocol step 4)

Each gap cross-references the premortem failure mode it closes. Dependency order: the four P0s (security/compliance perimeter) before the P1s (function) before the P2s (polish). **No code lands until this list is approved.**

### P0 — architectural / security

| # | Gap | Closes | Detail |
|---|---|---|---|
| **P0-1** | **Anon-EXECUTE sweep of the messaging definer functions** (the named §10 go-live rider). | FM-3 | **Re-probe live first** (`has_function_privilege('anon', …, 'EXECUTE')`, as sections 5–9 did — source-absence ≠ runtime-absence). Then one migration: `REVOKE EXECUTE … FROM anon` (and `authenticated`) on `client_cascade_thread_archive()` and `message_update_thread_last()`. Both are trigger-only — triggers fire as the table owner regardless of EXECUTE grants, so the revoke is behaviour-safe (verify the archive-cascade + bump still fire after). Add a `has_function_privilege` grants tripwire (fold into the P0-4 suite, e.g. `34_message_rls.sql §grants`) asserting anon=false. **Discharges the §10 slice** of the platform-wide rider; the remaining open item (`client_accept_invite` §2 pre-auth check) stays indexed in `go-live-checklist.md`. |
| **P0-2** | **Enforce message immutability + within-thread tamper-resistance at the DB layer** — make the migration header's claim true. *(Q2=(a) trigger; Q3=(a) no delete.)* | FM-1 | A `BEFORE UPDATE` trigger on `messages` that **raises unless only `read_at` changed** (Q3 = no user delete, so `deleted_at` is **not** user-mutable either) and that **forbids a client mutating a `sender_role='staff'` row** — clients get INSERT + mark-staff-message-read only. This pins `body`, `sender_role`, `sender_user_id`, `created_at`, `deleted_at`, `thread_id`, `organization_id` immutable for every API role. The only `deleted_at` writer stays the SECURITY DEFINER thread archive-cascade (which writes `message_threads`, not `messages`); if a message-level admin soft-delete is ever needed it goes through a new definer RPC, not a broadened policy. Tighten the UPDATE policies' `WITH CHECK` to match the trigger. Update the `messages.sql` header comment to describe the *actual* mechanism. Lock with pgTAP (P0-4). |
| **P0-3** | **Audit trail for `messages` + `message_threads`** — close the §7.4 / APP-compliance half of the deviation's approval condition. | FM-2 | Attach the standard audit trigger to both tables and **register both in `audit_resolve_org_id()`'s CASE list** so the coverage guard (`20260513160000`) passes (per the "audit-register new tables" rule — a trigger without the resolver branch writes NULL org_id). Extend [`14_audit_resolve_org_id_coverage.sql`](../../supabase/tests/database/14_audit_resolve_org_id_coverage.sql) to assert both branches exist. Verify an insert/soft-delete writes a correctly org-attributed audit row. |
| **P0-4** | **Messaging RLS regression suite** — the cross-tenant isolation execution gate for the section, and the tripwire for P0-1/P0-2. | FM-4 | New pgTAP (e.g. `34_message_rls.sql`) using the **JWT-spoof fixture pattern** (`create_test_session`-style, **not** SECURITY DEFINER bypass helpers — per the FORCE-RLS rule): (a) **cross-tenant** — org B cannot SELECT org A's threads/messages, cannot INSERT into A's thread; (b) **client scope** — a client sees only their own thread, cannot read another client's thread in the same org; (c) **immutability** — `UPDATE` of `body`/`sender_*`/`deleted_at` is rejected, a client cannot edit or soft-delete an EP message, `read_at` is the only mutable field (locks P0-2); (d) **grants** — `has_function_privilege` anon=false on the two definer funcs (locks P0-1). Runs in the live-project `BEGIN … ROLLBACK` harness. |

### P1 — functional

| # | Gap | Closes | Detail |
|---|---|---|---|
| **P1-1** | **New-message notification — three layers.** *(Q1 + Q5 resolved; see §0.1.)* | FM-5 (mitigates FM-7) | **(a) In-app indicator (Q1):** a top-right home-screen new-message indicator (unread count + state) on the staff `TopBar` bell and the client portal home (`PortalTop`) — extends the existing realtime unread badge into a prominent home-surface affordance. Ships first (cheapest). **(b) Web push (Q5), viability-gated:** a short spike first — confirm the PWA service worker, add VAPID keys + per-user push subscriptions, account for the **iOS-installed-PWA-only** limit — then deliver push for the surfaces where it's viable. **(c) Email fallback (recommended):** email-to-EP on a new `client→staff` message, reusing the verified Resend path + `EMAIL_FROM` and mirroring the reminder Edge-Function posture **including the standing synthetic-send check** (assert `succeeded≥1`, not just HTTP 200) — covers the iOS gap + any unsubscribed device; EP-direction only. **Debounce/rate-limit** all out-of-app channels (one nudge per thread per quiet-window). Trigger surface: a DB-trigger enqueue mirrors the scheduling reminder lifecycle and survives every send path — recommended over per-action enqueue. **Sequence:** (a) → (c) → (b). |
| **P1-2** | **Verify realtime RLS enforcement on live.** | FM-6 | Probe with two JWTs: subscribe as an org-B client to the `messages` channel and confirm **zero** org-A rows are delivered on an org-A insert; confirm the subscription is authorise-gated (private channel / RLS check), not anon-readable. Document the result in `rls-policies.md` (ties to P2-5). If it does **not** fail closed, switch to Supabase authorized/private channels before first real data. Verify-gate. |

### P2 — polish

| # | Gap | Closes | Detail |
|---|---|---|---|
| **P2-1** | **Clinical-safety disclosure hardening.** *(Q4=(a) confirmed.)* | FM-7 (residual) | Promote *"This channel isn't for urgent or clinical issues — for emergencies call 000, for clinical questions book an appointment"* to a **once-dismissed first-open line** in the thread, in addition to the persistent footer. Factual voice, no drama (per the design-system copy rules). |
| **P2-2** | **Thread-restore distinguishability.** | FM-8 | Accept for v1 (no thread-level archive UI exists, so the unconditional un-archive is harmless today). Re-trigger note recorded in §2. Revisit **only if** a thread-level archive action is added. |
| **P2-3** | **Design-token sweep of the messaging components.** | FM-9 | Replace inline raw px/rem/opacity literals in `Inbox.tsx` + `ClientThread.tsx` with `globals.css` tokens / existing classes; same discipline applied in the scheduling P2-13 pass. |
| **P2-4** | **Compose flow + copy.** | FM-10 | A pick-client composer for staff "New message" (instead of dead-ending at `/clients`); make "Reply within business hours" honest/configurable or generalise it; voice pass on the "say hello" empty states. Practitioner-configurable quick replies are a later nicety, not in this pass. |
| **P2-5** | **Document the messaging RLS in `rls-policies.md`** before the external-review gate. | FM-11 | Add the `messages` + `message_threads` policy set (both roles, both tables) and the realtime-RLS finding from P1-2 to `rls-policies.md`, so the Open-gates external IT review covers the highest-impact new surface. |

---

---

## 4. Progress log

### P0 — complete, applied to live, verified (2026-06-18)

Gap list approved by the operator 2026-06-18 (the five §0.1 questions resolved). All four P0 gaps are now closed. Three backward-compatible migrations were pushed to the live shared DB (`supabase db push`, applied in order); pgTAP [`34_message_rls.sql`](../../supabase/tests/database/34_message_rls.sql) (14/14 on live) is the regression tripwire, and [`14_audit_resolve_org_id_coverage.sql`](../../supabase/tests/database/14_audit_resolve_org_id_coverage.sql) still passes.

| Gap | Migration / test | Result |
|---|---|---|
| **P0-1** anon-EXECUTE sweep | [`20260618120000`](../../supabase/migrations/20260618120000_revoke_anon_execute_messaging_functions.sql) | Live probe **before**: `client_cascade_thread_archive()` and `message_update_thread_last()` both `anon=true, authenticated=true` (source ≠ runtime — the §5–9 pattern). **After**: both `false/false`. Trigger-only fns, so zero behavioural change. Discharges the **§10 slice** of the go-live anon-EXECUTE rider; `client_accept_invite` (§2) remains indexed in `go-live-checklist.md`. |
| **P0-2** immutability | [`20260618120100`](../../supabase/migrations/20260618120100_messages_immutability_trigger.sql) | `message_enforce_immutability()` `BEFORE UPDATE` freezes every column except `read_at` (Q3 = no user delete → `deleted_at` frozen too). Test 34/7 (client cannot edit the EP's body) + 34/9 (staff cannot) + 34/8 (read_at mark-read still works) green. The `messages.sql` immutability claim is now true. |
| **P0-3** audit trail | [`20260618120200`](../../supabase/migrations/20260618120200_audit_messages.sql) | `audit_messages` + `audit_message_threads` triggers attached; both tables added to `audit_resolve_org_id()` (canonical 20260513160000 body + 2 names); the migration-time `assert_audit_resolver_coverage()` passed (no raise); test 34/11 confirms an audit row is written with a resolved, non-NULL org. |
| **P0-4** RLS suite | [`34_message_rls.sql`](../../supabase/tests/database/34_message_rls.sql) | 14 assertions, **14 green** on live: cross-tenant read isolation (1-2), client self-scope (3-4), within-org client isolation (5-6), immutability (7,9), mark-read backward-compat (8), control (10), audit wiring (11), grant tripwires (12-14). |

**Verification:** `type-check` (tsc) clean; `supabase gen types` produced no diff (no table-shape change — triggers/functions/grants only). Committed to `polish/section-10-messaging`; **not** merged to master (no frontend change in P0, and the migrations are already live via `db push`, per the shared dev/prod-DB model). The closing commit + claude.ai sign-off follow at section end (after P1/P2), per the ritual.

**Next:** P1 — the three-layer notification stack (P1-1: in-app indicator → email fallback → web-push viability spike) and the realtime-RLS-on-live verification (P1-2).

### P1 — in progress

**P1-1(a) — in-app notification indicator: resolved to the existing nav indicators (operator :3000 review, 2026-06-20).**
- The first pass added a top-right bell to the portal home (`DayScreen`). The operator's :3000 review found it **redundant** with the bottom-nav Messages badge — the same unread signal twice on one screen, the clutter the design system warns against. **Reverted in this commit.** The in-app indicator is therefore the **existing** bottom-nav badge (client, on every portal screen) + the **existing** `TopBar` bell (staff, [`TopBar.tsx:166`](../../src/app/(staff)/_components/TopBar.tsx)) — no new indicator needed. This is the "existing code already satisfies the intent" outcome the polish protocol anticipates.
- **Notification-count colour — operator asked to switch the red badge to brand green; recommended keeping red, and the operator confirmed red (2026-06-20).** Reasoning: the design system reserves accent green for success/completion/structural and never for alerts; `--color-alert` red is the attention semantic; and on this very screen green already means "session scheduled / done" (the week-strip dots + the green "Completed" stat), so a green unread badge would collide with that established meaning. Red is also the platform convention for unread badges. *If overridden:* change `.portal-nav-badge` and `.topright .bell .count` together for consistency.

**P1-1(c) — email-to-EP on a new client message: done (2026-06-20).** A new client→staff message now emails the practice owner so an unread message isn't missed while the EP is offline (FM-5).
- **Approach — deviation from the gap doc's "DB-trigger + reminder-lifecycle" recommendation (surfaced for the record):** the email is sent best-effort from the one client send path (`sendClientMessageAction`) via Next's `after()` (post-response — never blocks or fails the client's send), **not** a queue + cron + Edge Function. Rationale: messages have a **single** client send path, so a trigger's "survives every path" benefit is marginal here; the action sends directly and surfaces Resend's result (no enqueue-vs-send blind spot — the exact trap the scheduling EF hit), and it avoids standing up another EF + cron + queue table + separate secret-set (a real operational cost, per the §9 close-out). **Trade-off:** no automatic retry — mitigated by the in-app unread indicator as the backstop. **Upgrade path:** move to the queue+cron pattern if beta feedback shows missed notifications.
- **Privacy:** the email carries **no message body**, only the client's first name — health-adjacent content stays inside the RLS/audit perimeter; the EP clicks through to read it in-app.
- **Debounce:** only the **first** unread client message in a thread emails (derived from `read_at`, no state table); a burst sends one nudge and re-arms once the EP reads.
- **Recipient:** the org owner(s) — the EP — resolved via the service-role client + `auth.admin.getUserById` (`user_profiles` carries no email, so `auth.users` is canonical). Files: [`templates/message-notification.ts`](../../src/lib/email/templates/message-notification.ts), [`send-message-notification.ts`](../../src/lib/email/send-message-notification.ts), [`portal/messages/actions.ts`](../../src/app/portal/messages/actions.ts).
- **Verification:** `type-check` + `next build` clean. The send reuses the proven Resend path (`getResendClient`/`defaultFromAddress`, identical to the verified booking-confirmation email); the live trigger→email is exercised when a client messages (operator test: sign in as a client, send a message, check the EP inbox).

**P1-1(b) — web-push spike (per operator's spike-then-decide): viable, but recommend deferring for the beta.**
- *Foundation that exists:* the portal is an installable PWA — manifest, icons, HTTPS (prod), and a registered service worker ([`public/sw.js`](../../public/sw.js), scope `/portal/`). The SW is minimal (install/activate/no-op fetch) — **no `push`/`notificationclick` handlers yet**. The staff side has **no** service worker at all.
- *What a real build needs:* (1) a **VAPID key pair** — the private key is a **new stored secret**; (2) `push` + `notificationclick` handlers in `sw.js`; (3) a client subscribe flow (permission prompt + `pushManager.subscribe`); (4) a `push_subscriptions` table (RLS + audit-registered → a migration); (5) a server send path (`web-push` lib, signed payloads), reusing the P1-1c debounce/recipient logic; (6) tasteful permission UX. For EP-**desktop** push specifically, a **new staff-side service worker** too (the existing SW is portal-only).
- *Reach (the iOS caveat, confirmed):* desktop (Chrome/Edge) — full; Android clients — full; **iPhone clients — only if the PWA is installed to the home screen (iOS 16.4+)**; a Safari-tab client gets none.
- *Recommendation — defer for the friends-and-family beta.* The email layer (P1-1c) now guarantees the EP never misses a client message, and the in-app badge covers in-app; together they close FM-5. Web push adds a new secret + a table + SW work (client *and* staff) + a permission surface, for partial extra reach (iOS install-gated on the client side). It's nice-to-have polish, not a gap-closer. **Re-trigger:** beta feedback shows email + in-app aren't enough, or the EP specifically wants desktop OS notifications. Decision is the operator's (spike-then-decide).

**P1-2 — realtime tenant-isolation verify: largely discharged + a first-accounts verify-gate.** The property reduces to (1) the RLS SELECT policies being correct and (2) the realtime server enforcing them per-subscriber.
- **(1) is PROVEN** by pgTAP [`34_message_rls.sql`](../../supabase/tests/database/34_message_rls.sql): tests 1-2 (cross-tenant) + 5-6 (within-org client) assert exactly the SELECT isolation that `postgres_changes` gates delivery through — realtime uses the **same** SELECT policy.
- **(2) is the Supabase default** for `postgres_changes` when RLS is on (live-confirmed: both tables have RLS *and* are in the `supabase_realtime` publication), and the browser client (`@supabase/ssr`) auto-syncs the authenticated user's JWT to the realtime socket, so a subscription carries the user's claims (an anon socket gets anon RLS = zero message rows, since `messages` has no anon policy).
- **Residual (verify-gate):** a live two-session E2E probe (subscribe as a foreign session, confirm zero foreign rows on a foreign insert) needs two real client sessions, which don't exist until the first friends-and-family test accounts do. Since (1) is proven and the setup is verified, this only confirms the realtime server applies the (already-proven) policy. **Run with the first test accounts; promote to `go-live-checklist.md` at section close.**

**P1 status: P1-1(a) resolved, P1-1(c) done, P1-2 discharged (+ gate); P1-1(b) spiked → awaiting the operator's defer/build decision. P2 follows.**

---

*Protocol status: steps 1–4 complete; gap list **approved** 2026-06-18; **P0 done + verified on live**; **P1 nearly complete** — P1-1(a) resolved (nav indicators suffice; badge stays red, operator-confirmed), P1-1(c) email done, P1-2 realtime discharged + gated; **P1-1(b) web push spiked, awaiting the operator's defer/build call**. P2 follows, then the closing commit + claude.ai sign-off.*
